# 项目约定

## 基本规则

- 对话、提交说明和项目说明默认使用简体中文。
- 所有 shell 命令必须带 `rtk` 前缀。需要原始输出时使用 `rtk proxy <cmd>`。
- 优先修改现有模块，不新建平行实现；插件边界已经集中在 `coding_orchestration/`。
- 不把运行根、token、auth、`.env*`、本地 LLM Wiki 内容或 Task Ledger 数据提交到仓库。
- 飞书项目 MCP 只读取 `~/.hermes/coding-orchestration/mcp.json`；真实 `MCP_USER_TOKEN` 只允许存在于本机运行配置和 MCP 子进程环境，不允许写入仓库、LLM Wiki、run artifacts、prompt 或测试 fixture。

## 开发入口

- 插件注册入口是 `coding_orchestration/__init__.py`。
- `/coding` 命令执行和人机门禁主要在 `coding_orchestration/orchestrator.py`；普通 `/coding ...` command façade 分发表、command-level wrapper、delivery rollup/materialize helper、artifact purge callback 和 prepare-merge-test status callback 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_command_facade.py`；source context 读取、deferred source pre-run enrichment 和既有 task context 修复优先维护在 `coding_orchestration/source_context_repair_service.py`；Kanban create/status sync host helper、skipped/failed sync record 和 task status view 字段投影优先维护在 `coding_orchestration/kanban_sync_service.py`；active coding status 列表、取消态保护文案、cancelled task 恢复状态选择和 merged_test bugfix reopen 优先维护在 `coding_orchestration/task_lifecycle_guard_service.py`；Gateway 后台 run mode-specific 启动和 completion notification 接线优先维护在 `coding_orchestration/coding_background_run_executor.py`；后台 run 等待完成、后台失败 transition 和 merge-test `human_required` 转 pending action 优先维护在 `coding_orchestration/run_background_orchestration.py`；runner/checkpoint failure report payload 优先维护在 `coding_orchestration/run/projections/run_failure_report_projection.py`；diff guard / implementation commit missing blocked report 构造和 run report refinement projection 优先维护在 `coding_orchestration/run/projections/run_report_refinement_projection.py`；run diff guard observation 优先维护在 `coding_orchestration/run_diff_guard_service.py`；checkpoint failure gate、`runner.run()` 调度和 runner exception fallback 优先维护在 `coding_orchestration/run_dispatch_service.py`；状态机、status/phase/Kanban callback、run start/completion/reconcile transition 和 active run cleanup 优先维护在 `coding_orchestration/run_status_transition_service.py`；run context source、run checkpoint 选择、QA evidence observation、source branch recording、project path requirement、workspace selection 和 manifest checkpoint preparation selection 纯规则优先维护在 `coding_orchestration/run/projections/run_start_selection_projection.py`；plan report session fields 白名单、plan report session update、run start base/workspace session update、active run session update、runner session update 和 completion session update 优先维护在 `coding_orchestration/run/projections/run_session_projection.py`，对应 task session callback 优先维护在 `coding_orchestration/run/services/run_session_writeback_service.py`；首次/增量 prompt 构造选择和参数合同优先维护在 `coding_orchestration/run/projections/run_prompt_projection.py`；run context artifact 写入和 execution policy artifact 读取优先维护在 `coding_orchestration/run/artifacts/run_context_artifact_service.py`；`report.schema.json`、`input-prompt.md` 和 `run-manifest.json` 启动 artifact 写入优先维护在 `coding_orchestration/run/artifacts/run_start_artifact_service.py`；后续 `run-manifest.json` artifact 写回优先维护在 `coding_orchestration/run/artifacts/run_manifest_artifact_service.py`；runner session metadata 到 manifest 的 host 写回优先维护在 `coding_orchestration/run/services/run_manifest_session_writeback_service.py`；`stderr.log` 写回优先维护在 `coding_orchestration/run/artifacts/run_stderr_artifact_service.py`；`report.json` 读写和 summary excerpt 优先维护在 `coding_orchestration/run/artifacts/run_report_artifact_service.py`；`summary.md` 读写优先维护在 `coding_orchestration/run/artifacts/run_summary_artifact_service.py`；start_run 与 active run reconcile 的 run ledger 写回 payload 聚合优先维护在 `coding_orchestration/run/projections/run_ledger_projection.py`；对应 ledger append/upsert callback 优先维护在 `coding_orchestration/run/services/run_ledger_writeback_service.py`；fresh/existing run 的 `ArtifactSet` 路径合同优先维护在 `coding_orchestration/run/artifacts/run_artifact_paths.py`；completed run 与 active run reconcile 的 summary writer callback 优先维护在 `coding_orchestration/run/services/run_summary_writeback_service.py`；completed run 的 Project writeback stale gate 和 callback 调用优先维护在 `coding_orchestration/run/services/run_project_writeback_service.py`；启动期 manifest update projection、run-manifest 基础字段和权限 profile 优先维护在 `coding_orchestration/run_manifest_service.py`；observed run report 构造、stale completion 观测、execution policy decision 读取、run-level diff guard violations 组合、verification limitations fallback projection、completion report payload、agent run record 构造、reconciled agent run record 构造、reconcile result payload 构造、existing run mode/changed files 规则、merge-test run record 构造、project writeback payload 构造、start_run result payload 构造和 run completion 状态/phase/report 投影优先维护在 `coding_orchestration/run_orchestration_service.py`，session/prompt projection 仅作为兼容 re-export 暴露；显式 `/coding` / `/commands` 解析、命令归一化、命令 route plan、handler key、reply mode、task id 来源策略、merge-test 参数、rewrite canonical command、确认/取消词、rewrite 风险确认、Gateway event dedupe 和授权探测优先维护在 `coding_orchestration/gateway/gateway_command_controller.py`；task/run/delivery/implementation/QA/prepare-merge-test/merge-test 的 Gateway custom route 分发优先维护在 `coding_orchestration/gateway/gateway_command_executor.py`；Gateway coding mode enter/exit、pending rewrite 和 rewrite handoff host shell 优先维护在 `coding_orchestration/gateway/gateway_coding_mode_executor.py`；pending action 确认/取消、latest human_required fallback 和确认后显式命令续接优先维护在 `coding_orchestration/gateway/gateway_pending_action_executor.py`；普通 run/implement/QA command-mode host shell 优先维护在 `coding_orchestration/coding_commands/coding_run_command_executor.py`；普通 continue/change/bugfix feedback command host shell 和记录 helper 优先维护在 `coding_orchestration/coding_commands/coding_feedback_command_executor.py`；普通 use/exit/cancel/restore/delete/complete task control host shell 优先维护在 `coding_orchestration/coding_commands/coding_task_control_command_executor.py`；active project 应用到缺项目 task 的回填逻辑优先维护在 `coding_orchestration/gateway/gateway_active_context.py`。
- Tool operation façade、`tool_*` 入口、tool run/source preflight adapter 和 Project MCP / WorkItem 小包装优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_tool_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。
- Bootstrap façade、`__post_init__`、`set_dispatch_tool`、`from_default_config` 和默认运行根 helper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_bootstrap_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。`handle_gateway_event()`、`pre_llm_call()`、`start_run()`、runner dispatch、workspace/git/checkpoint 实现和 run lifecycle 仍按各自边界维护。
- Diagnostics / dashboard façade、doctor/CLI wrapper、Meegle preflight fallback、dashboard status payload 和 preflight/source-resolve formatter wrapper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_diagnostics_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。
- Gateway route façade、immediate/custom route dispatch、Coding Mode rewrite wrapper、pending action/rewrite binding wrapper、确认/取消词 wrapper、commands listing route、dedupe/授权/looks-like-task wrapper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_gateway_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。`handle_gateway_event()` 入口、task/source façade、active task/project binding 业务、runner/workspace/git 和 run lifecycle 仍按各自边界维护。
- Project/profile façade、project profile catalog wrapper、active project binding wrapper、本地项目候选/路径/别名 wrapper、active project 回填 wrapper、人工项目画像 upsert 和项目澄清 host callback 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_project_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。task/source façade、Gateway route、active task/coding mode binding、runner/workspace/git 和 run lifecycle 仍按各自边界维护。
- Task/source façade、task create command wrapper、TaskService source/status payload wrapper、source context 读取/修复/enrich wrapper、event/media ledger helper 和 draft knowledge source refs wrapper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_task_source_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。active task/coding mode binding、feedback command host shell、runner/workspace/git 和 run lifecycle 仍按各自边界维护。
- Task runtime façade、Kanban/status host wrapper、task list/status event wrapper、feedback active-task wrapper、active task/coding mode binding wrapper、task lifecycle wrapper、人工决策记录和 RunService gate wrapper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_task_runtime_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。`handle_gateway_event()`、active run reconcile、runner/workspace/git、`start_run()` 和 run lifecycle 仍按各自边界维护。
- Workspace façade、implementation/merge-test workspace 查找、QA artifact 收集、QA/merge-test checkpoint 准备、dirty/head/clean checkpoint 观测、source branch/base branch 投影、task short id/slug 和 latest existing implementation workspace wrapper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_workspace_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。workspace/git/checkpoint 核心实现仍归 `workspace_checkpoint_service.py`，`handle_gateway_event()`、active run reconcile、runner 调度、`start_run()` 和 run lifecycle 仍按各自边界维护。
- Background façade、后台 plan/implementation/QA/merge-test 启动与通知 wrapper、completion wait/failed/pending-action/notification wrapper、sender scheduling/reply wrapper 和 execution policy artifact wrapper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_background_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。后台 executor/notifier/orchestration 核心实现仍归 `coding_background_run_executor.py`、`background_run_notifier.py` 和 `run_background_orchestration.py`，`handle_gateway_event()`、active run reconcile、runner 调度、`start_run()` 和 run lifecycle 仍按各自边界维护。
- Status policy façade、report status details、implementation status normalize、verification limitations 补齐和 run result 到 task status/phase 映射 wrapper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_status_policy_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。`status_policy.py` 纯规则、状态机、active run reconcile、runner 调度、`start_run()` 和 run lifecycle 仍按各自边界维护。
- Manifest façade、existing/fresh artifact set、run manifest 构造、Codex attach/resume 命令、session id 读取、runner manifest name 和 manifest session metadata 写回 wrapper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_manifest_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。manifest 纯规则、artifact service、active run reconcile、runner 调度、`start_run()` 和 run lifecycle 仍按各自边界维护。
- Prompt/context façade、resumed session 增量上下文、wiki docs/refs、context artifacts、dependency/sibling task context、confirmed plan / merge-test context、report summary markdown 和 wiki ref 投影 wrapper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_prompt_context_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。context artifact service 纯写入、prompt projection、manifest 构造、runner 调度、`start_run()` 和 run lifecycle 仍按各自边界维护。
- Merge-test façade、merge-test blocker、blocked task 风险放行 host 写回、blocked assessment fact collection、latest implementation run/source branch/disallowed reason wrapper 和 QA evidence wrapper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_merge_test_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。merge-test readiness 纯评估规则、workspace/git/checkpoint 实现、runner 调度、`start_run()` 和 run lifecycle 仍按各自边界维护。
- Runtime/failure façade、flag helper、project/workflow wrapper、plan report session/execution policy/timeout wrapper、runner failed result 和 checkpoint failed result 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_runtime_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。`start_run()` 主体、runner dispatch、artifact service 纯写入、workspace/git/checkpoint 实现和 run lifecycle 仍按各自边界维护。
- Active run façade、completed active run reconcile host shell 和 agent run lookup helper 优先维护在 `coding_orchestration/orchestrator_facades/orchestrator_active_run_facade.py`；不要把这组 wrapper 恢复到 `orchestrator.py` 主文件。`start_run()` 主体、fresh completion writeback、runner dispatch、workspace/git/checkpoint 实现和 run lifecycle 仍按各自边界维护。
- Completed run 与 active run reconcile 的 run summary writer payload 聚合优先维护在 `coding_orchestration/run/projections/run_summary_projection.py`；该模块只返回写入参数，不读取 summary artifact、不调用 summary writer、不写 LLM Wiki、不写 ledger、不推进状态。
- Completed run 与 active run reconcile 的 summary writer host callback 优先维护在 `coding_orchestration/run/services/run_summary_writeback_service.py`；该模块只消费 summary projection 并调用注入 writer callback，不读取 `summary.md`、不直接 import `RunSummaryWriter` / `KnowledgePort`、不写 ledger/artifact、不启动 runner、不推进状态。
- Completed run 与 active run reconcile 的 run ledger host callback 优先维护在 `coding_orchestration/run/services/run_ledger_writeback_service.py`；该模块只消费 `run_ledger_projection.py` 生成的 records 并调用注入 ledger callback，不构造 payload、不直接 import `TaskLedger` / storage repository、不写 artifact、不启动 runner、不推进状态。
- Start run 与 active run reconcile 的 run session host callback 优先维护在 `coding_orchestration/run/services/run_session_writeback_service.py`；该模块只消费 session projection 生成的 update dict 并调用注入 task session callback，空 update 跳过，不构造 payload、不直接 import `TaskLedger` / storage repository、不写 artifact、不启动 runner、不推进状态。
- Run diff guard observation 优先维护在 `coding_orchestration/run_diff_guard_service.py`；该模块只执行 before snapshot、changed files 观测、QA report artifact 过滤、policy violations 组合和指定 diff summary 写回，不写 ledger/report/manifest/summary、不启动 runner、不收集 QA evidence、不推进状态。
- Runner dispatch 优先维护在 `coding_orchestration/run_dispatch_service.py`；该模块只消费已准备好的 checkpoint，调用注入 runner 或失败 result callback，不写 ledger/report/manifest/summary、不执行 diff guard、不收集 QA evidence、不推进状态。
- Run status transition 优先维护在 `coding_orchestration/run_status_transition_service.py`；该模块只消费已计算好的目标 status/phase 和注入 callback，允许调用 `TaskStateMachine`，但不读取 report、不 append/upsert run ledger records、不启动 runner、不执行 diff guard、不收集 QA evidence。
- Run evidence observation 优先维护在 `coding_orchestration/run_evidence_observation_service.py`；该模块只调用注入 callback 观测 QA artifacts、tested commit 和 implementation dirty-check，不写 ledger/report/manifest/summary、不创建 checkpoint、不启动 runner、不执行 diff guard、不推进状态。
- Run checkpoint preparation 优先维护在 `coding_orchestration/run_checkpoint_preparation_service.py`；该模块只调用注入 callback 准备 QA / merge-test checkpoint，并返回 `manifest_updates`，不直接 mutate manifest、不写 artifact/ledger/report/summary、不启动 runner、不执行 diff guard、不推进状态。
- Run implementation checkpoint writeback 优先维护在 `coding_orchestration/run_implementation_checkpoint_service.py`；该模块只消费已计算好的 dirty flag，调用注入 checkpoint / manifest writer callback，不判断 dirty、不构造 blocked report、不写 ledger/report/summary、不启动 runner、不推进状态。
- Run manifest session metadata writeback 优先维护在 `coding_orchestration/run/services/run_manifest_session_writeback_service.py`；该模块只消费已解析 session id，复用 `run_manifest_service.py` 的字段投影，更新 manifest object/dict 并调用注入 manifest metadata writer，不解析 stdout、不写 ledger/report/summary、不启动 runner、不推进状态。
- Fresh completed run writeback coordinator 优先维护在 `coding_orchestration/run_completion_writeback_service.py`；该模块只消费已完成 runner result 的投影数据和注入 callback，协调 report artifact、状态 transition、ledger/session/summary/project writeback 与 result payload，不启动 runner、不执行 diff guard、不收集 QA evidence、不判断 dirty、不准备 checkpoint、不解析 stdout、不直接持有 host adapter。
- Hermes native tools 注册在 `coding_orchestration/plugin_tools.py`，工具 operation 分发在 `coding_orchestration/tool_operation_dispatcher.py` 的 `ToolOperationDispatcher`，CLI 子命令注册在 `coding_orchestration/cli.py`。`plugin_tools.py` 只做 host payload 归一和 `ToolSpec.operation_id` handler 包装，不维护 `operation_id -> CodingOrchestrator.tool_*` 方法映射；`cli.py` 中 `lark-preflight`、`source-resolve`、普通 `status <task_id>` 和带 host config gate 的 `project-mcp-preflight` 直接走 `dispatch_tool_operation()`；Gateway `/coding project-mcp-preflight` 只作为 diagnostic leaf 复用同一 operation 边界。future MCP tool 只有出现真实消费方后再接入，不作为当前未完成项。
- Delivery command host shell 优先维护在 `coding_orchestration/commands/delivery/delivery_command_executor.py`，Project command host shell 优先维护在 `coding_orchestration/commands/project/project_command_executor.py`；二者只承接用户可见命令分支和 host callback 绑定，不实现 Gateway route、runner/workspace/git、状态机或 run lifecycle。
- 普通 `/coding help`、Gateway `/commands` listing、分页和 Hermes built-in command list 拼接优先维护在 `coding_orchestration/coding_commands/coding_help_command_executor.py`；command catalog 纯数据继续维护在 `coding_orchestration/command_catalog.py`，不要把 help/listing 拼接逻辑写回 `orchestrator.py`。
- 普通 `prepare-merge-test` / `merge-test` command host shell、blocked risk release 接线、QA 风险确认和 merge-test run 启动 callback 优先维护在 `coding_orchestration/coding_commands/coding_merge_test_command_executor.py`；该 executor 直接消费 `coding_orchestration/presenters/merge_test_presenter.py` / `coding_orchestration/presenters/run_completion_presenter.py`，workspace/git/checkpoint 实现、blocked readiness 纯评估、QA evidence 观测、`start_run()` 实现和 run lifecycle 仍留在既有边界。
- Gateway 后台 plan-only / implementation / QA / merge-test 的 mode-specific 启动、completion message 选择、失败通知接线和 completion notification callback 优先维护在 `coding_orchestration/coding_background_run_executor.py`；`start_run()`、后台等待完成、失败状态收敛、merge-test pending action、runner/workspace/git/checkpoint 和 run lifecycle 仍留在既有边界。
- Run start 用户可见文案优先维护在 `coding_orchestration/presenters/run_start_presenter.py`；`gateway_command_executor.py`、`gateway_coding_mode_executor.py`、`coding_run_command_executor.py` 和 `RunService` 直接调用 presenter，不要再通过 `CodingOrchestrator._plan_only_started_message`、`_implementation_started_message`、`_active_run_already_running_message` 或同类私有代理中转。
- Run completion 用户可见文案优先维护在 `coding_orchestration/presenters/run_completion_presenter.py`；`coding_run_command_executor.py`、`coding_merge_test_command_executor.py` 和 `coding_background_run_executor.py` 直接调用 presenter，不要再通过 `CodingOrchestrator._format_*completion_message` 或 completion helper 私有代理中转。
- Merge-test 用户可见文案优先维护在 `coding_orchestration/presenters/merge_test_presenter.py`；`coding_merge_test_command_executor.py` 和 `gateway_command_executor.py` 直接调用 presenter，不要再通过 `CodingOrchestrator._blocked_merge_test_risk_confirmation_message`、`_merge_test_qa_risk_confirmation_message`、`_blocked_merge_test_release_note`、`_fallback_evidence_user_line`、`_merge_test_started_message` 或同类私有代理中转。
- Doctor/preflight/source-resolve 用户可见文案优先维护在 `coding_orchestration/presenters/doctor_presenter.py`；`coding_diagnostics_command_executor.py` 直接调用 presenter，不要再通过 `CodingOrchestrator._doctor_lark_summary`、`_doctor_project_mcp_summary`、`_doctor_runtime_summary`、`_doctor_codex_summary`、`_doctor_display_scope`、`_doctor_status_label`、`_doctor_scope_login_hint`、`_doctor_extract_rtk_command` 或同类私有代理中转。
- Gateway diagnostic route 保持在 `coding_orchestration/gateway/gateway_command_controller.py` / `orchestrator.py` 的 host reply 边界；`/coding project-mcp-preflight` 只作为 immediate reply 复用 project MCP preflight façade，不把 `doctor` 聚合或 `status` presentation 下沉为单个 tool operation。
- 本地项目文件夹识别、搜索根去重、路径解析和人工别名抽取优先维护在 `coding_orchestration/gateway/gateway_project_context.py`；orchestrator 只保留兼容 wrapper 和默认 fallback，不把 Gateway 回复、profile 写入或 active project binding 塞入该 helper。
- Source context 读取、deferred source pre-run enrichment、既有 task context 修复、source summary 回写和项目上下文回填接线优先维护在 `coding_orchestration/source_context_repair_service.py`；legacy `source_context` schema、source reader 和 source 权限策略仍留在既有 source/service 边界。
- Kanban create/status sync host helper、skipped/failed sync record 和 task status view 字段投影优先维护在 `coding_orchestration/kanban_sync_service.py`；真实工具映射继续归 `coding_orchestration/kanban_bridge.py`，状态机和 transition 仍归既有 run status 边界。
- Active coding status 列表、取消态保护文案、cancelled task 恢复状态选择和 merged_test bugfix reopen 优先维护在 `coding_orchestration/task_lifecycle_guard_service.py`；状态转换合法性仍归状态机，runner/workspace/git 仍归既有 run 边界。
- blocked task 是否可继续 merge-test 的 readiness 评估优先维护在 `coding_orchestration/merge_test_readiness_service.py`；该模块只消费 task/run/workspace/session/report 事实并返回评估 dict，不写 ledger、不推进状态、不触发 runner、不发送 Gateway 回复。
- project profile 读取、registry fallback、别名/路径查找、动态来源计数和 project list/status 格式化优先维护在 `coding_orchestration/project_profile_catalog.py`；project init/upsert、active project binding 和 Gateway 回复副作用仍留在 orchestrator host 边界。
- 通用 skill 规则维护在 `coding_orchestration/skills/coding-operator-core/` 和 `coding-health-core/`；Hermes 绑定映射维护在 `hermes-coding-operator/` 和 `hermes-coding-health-check/`。
- Codex CLI 命令构造、resume、sandbox、结构化 report 读取在 `coding_orchestration/runners/codex_cli.py`。
- 安装和卸载逻辑优先改 `coding_orchestration/install.py`，脚本只保留入口和用户输出。

## 验证 Gate

发布 readiness gate 统一入口是 `rtk proxy python3 scripts/release_readiness.py`：默认串联完整单测、architecture guard、`git diff --check`、敏感扫描和最小 Hermes smoke；本机未运行 Hermes 时可显式追加 `--skip-hermes-smoke`，但正式发布前必须补跑 smoke。

| 场景 | 命令 |
| --- | --- |
| 完整单测 | `rtk proxy python3 -m unittest discover -s tests -v` |
| 单个测试文件 | `rtk proxy python3 -m unittest tests.test_install -v` |
| 架构治理检查 | `rtk proxy python3 scripts/architecture_guard.py` |
| 安装前置检查 | `rtk python3 scripts/install_symlink.py --hermes-home ~/.hermes` |
| 卸载 dry-run | `rtk python3 scripts/uninstall_legacy.py --hermes-home ~/.hermes` |
| Hermes 插件状态 | `rtk hermes plugins list` |
| Hermes Gateway 状态 | `rtk hermes gateway status` |
| Gateway health | `rtk proxy curl -sS http://127.0.0.1:8642/health` |

运行安装脚本会访问本机 Hermes、Codex CLI、`lark-cli` 和飞书权限状态；在普通代码改动中，默认先跑单测。只有变更安装链路或本机联调时才执行安装脚本。

## 测试约定

- 测试使用标准库 `unittest`，测试文件位于 `tests/test_*.py`。
- 新增状态、命令、runner、source resolver、report gate 或用户可见文案时，应补充相邻测试。
- 旧私有 helper / 旧文件形态测试已完成最终清理：`tests/test_orchestrator_run_flow.py` 只保留端到端 smoke、kanban transition 和 plan-only resume sandbox；新增或迁移覆盖优先落到 service/policy/presenter/adapter contract tests 或行为域小文件，`tests/test_architecture_guard.py` 锁定 resolved legacy flow suite 不再保留 large-file 豁免。
- 变更父级需求拆解、materialize、delivery status 或 run --next 行为时优先扩展 `tests/test_delivery_service.py`、`tests/test_delivery_command_executor.py` 和 `tests/test_delivery_flow.py`。
- 变更飞书 Project/Docx/Wiki 来源索引、deferred source、source context 修复或 plan 阶段来源读取权限时，优先扩展 `tests/test_source_flow.py`、`tests/test_source_plan_flow.py` 和相邻 source contract tests。
- 变更 `SourcePort`、`SourceResult`、`SourceResolver.resolve_source_result()` 或 source 状态归一化时，优先扩展 `tests/test_ports_contract.py`、`tests/test_source_resolver.py` 和 `tests/test_orchestrator_tools.py`。
- 变更 `SourceProjection` 字段、legacy `source_context` 到稳定来源投影的映射、prompt source block 来源状态展示、dashboard source health 统计、source-resolve diagnostic payload、task creation helper 或 run 前 deferred source enrichment 时，优先扩展 `tests/test_source_projection.py`、`tests/test_prompt_templates.py`、`tests/test_dashboard_api_contract.py`、`tests/test_task_service.py` 和 `tests/test_source_plan_flow.py`；再按影响范围扩展 `tests/test_source_flow.py`、`tests/test_run_manifest_service.py` 或 `tests/test_run_context_artifact_service.py`。不要在首切片改 ledger schema 或 `source_context` 持久化结构。
- 变更 source context 读取、deferred source pre-run enrichment、既有 task context 修复、source summary 回写或项目上下文回填接线时，优先扩展 `tests/test_source_context_repair_service.py`，再按影响范围扩展 `tests/test_source_flow.py`、`tests/test_source_plan_flow.py`、`tests/test_gateway_project_task_flow.py`、`tests/test_task_service.py` 或 `tests/test_run_manifest_service.py`。
- 变更 `coding_orchestration/feishu/` 内 Feishu Docx/Wiki gateway 读取、`lark-cli docs +fetch`、auth refresh retry 或文档 payload 归一化时，优先扩展 `tests/test_feishu_document_reader.py`；变更 Feishu Project work item gateway/OpenAPI env 读取时，优先扩展 `tests/test_feishu_work_item_reader.py`；变更 Feishu/Meegle work item payload 归一化、raw_fields 或 summary shape 时，优先扩展 `tests/test_source_work_item_context.py`；`tests/test_feishu_project_reader.py` 只保留 Project/文档来源路由兼容覆盖。
- 变更 plan-only run、计划完成通知、只读 gate 或 run summary 写入时，优先扩展 `tests/test_plan_run_flow.py` 和相邻 `RunService` / prompt contract tests。
- 变更 implementation 结果映射、workspace/branch 策略、session 复用、confirmed plan prompt 或 inline implementation 策略时，优先扩展 `tests/test_implementation_result_flow.py`、`tests/test_implementation_workspace_flow.py`、`tests/test_implementation_session_flow.py` 和相邻 runner / status policy tests。
- 变更 `/coding run`、`/coding implement`、后台启动拒绝或已完成任务 run gate 时，优先扩展 `tests/test_command_run_flow.py` 和相邻 `RunService` tests。
- 变更后台 run 线程启动、sender 调度、reply fallback、失败通知模板或 completion notification record 时，优先扩展 `tests/test_background_run_notifier.py`，再按影响范围扩展 `tests/test_plan_run_flow.py`、`tests/test_qa_flow.py`、`tests/test_merge_test_qa_gate_flow.py` 或 `tests/test_command_run_flow.py`。
- 变更 Gateway 后台 run mode-specific 启动、completion message 选择、失败通知接线或 completion notification callback 时，优先扩展 `tests/test_coding_background_run_executor.py`，再按影响范围扩展 `tests/test_background_run_notifier.py`、`tests/test_plan_run_flow.py`、`tests/test_qa_flow.py`、`tests/test_merge_test_qa_gate_flow.py` 或 `tests/test_command_run_flow.py`。
- 变更后台 run 等待完成、后台启动失败状态收敛、merge-test `human_required` 转 pending action 或对应 orchestrator wrapper 时，优先扩展 `tests/test_run_background_orchestration.py`，再按影响范围扩展 `tests/test_plan_run_flow.py`、`tests/test_command_run_flow.py`、`tests/test_status_reconcile_flow.py`、`tests/test_qa_flow.py` 或 `tests/test_merge_test_qa_gate_flow.py`。
- 变更 run start selection projection，也就是 run context source、QA/merge-test checkpoint 选择、checkpoint failed 判定、QA evidence observation、source branch recording、project path requirement、workspace selection 或 manifest checkpoint preparation selection 时，优先扩展 `tests/test_run_start_selection_projection.py`，再按兼容影响扩展 `tests/test_run_orchestration_start_rules.py` 或 `tests/test_run_orchestration_workspace_rules.py`。变更 `report.schema.json`、`input-prompt.md` 或 `run-manifest.json` 启动 artifact 文件写入时，优先扩展 `tests/test_run_start_artifact_service.py`，再按影响范围扩展 `tests/test_plan_run_flow.py`、`tests/test_source_plan_flow.py`、`tests/test_implementation_session_flow.py`、`tests/test_qa_flow.py` 或 `tests/test_merge_test_basic_flow.py`；变更 `run-manifest.json` artifact 写回边界时，优先扩展 `tests/test_run_manifest_artifact_service.py`，再按影响范围扩展 `tests/test_implementation_session_flow.py`、`tests/test_implementation_workspace_flow.py`、`tests/test_qa_flow.py` 或 `tests/test_merge_test_basic_flow.py`；变更 `stderr.log` artifact 写回边界时，优先扩展 `tests/test_run_stderr_artifact_service.py`，再按影响范围扩展 `tests/test_run_failure_report_projection.py`、`tests/test_run_orchestration_start_rules.py`、`tests/test_qa_flow.py` 或 `tests/test_merge_test_basic_flow.py`；变更 `report.json` artifact 读写边界时，优先扩展 `tests/test_run_report_artifact_service.py`，再按影响范围扩展 `tests/test_status_reconcile_flow.py`、`tests/test_plan_run_flow.py`、`tests/test_source_plan_flow.py`、`tests/test_implementation_session_flow.py`、`tests/test_qa_flow.py` 或 `tests/test_merge_test_basic_flow.py`；变更 `summary.md` artifact 读写边界时，优先扩展 `tests/test_run_summary_artifact_service.py`，再按影响范围扩展同一组 run/status/QA/merge-test flow。
- 变更 runner/checkpoint failure report payload 时，优先扩展 `tests/test_run_failure_report_projection.py`，并按兼容影响扩展 `tests/test_run_orchestration_start_rules.py`。变更 diff guard / implementation commit missing blocked report 构造或 run report refinement projection 时，优先扩展 `tests/test_run_report_refinement_projection.py`，并按兼容影响扩展 `tests/test_run_orchestration_start_rules.py`；变更 run diff guard snapshot、changed files 观测、QA report artifact 过滤、policy violations 组合或 diff summary 写回时，优先扩展 `tests/test_run_diff_guard_service.py`，再按影响范围扩展 `tests/test_diff_guard.py`、`tests/test_plan_run_flow.py` 或 `tests/test_implementation_workspace_flow.py`；变更 QA artifact/tested commit observation 或 implementation dirty-check observation 时，优先扩展 `tests/test_run_evidence_observation_service.py`，再按影响范围扩展 `tests/test_qa_flow.py`、`tests/test_implementation_workspace_flow.py` 或 `tests/test_run_orchestration_start_rules.py`；变更 QA/merge-test checkpoint preparation callback 选择、调用或 manifest update payload 时，优先扩展 `tests/test_run_checkpoint_preparation_service.py`，再按影响范围扩展 `tests/test_run_start_selection_projection.py`、`tests/test_qa_flow.py`、`tests/test_merge_test_basic_flow.py` 或 `tests/test_merge_test_qa_gate_flow.py`；变更 implementation dirty 后置 checkpoint 生成或 manifest artifact writeback callback 接线时，优先扩展 `tests/test_run_implementation_checkpoint_service.py`，再按影响范围扩展 `tests/test_implementation_workspace_flow.py`、`tests/test_run_manifest_artifact_service.py`、`tests/test_run_report_refinement_projection.py` 或 `tests/test_run_orchestration_start_rules.py`。变更 plan report session fields / writeback、run start base/workspace session update、active run session update、runner session update 或 completion session update 时，优先扩展 `tests/test_run_session_projection.py`，兼容 re-export 行为再扩展 `tests/test_run_orchestration_plan_report_session.py` 或 `tests/test_run_orchestration_start_rules.py`；实际 task session callback 调用归属 `tests/test_run_session_writeback_service.py` / `run/services/run_session_writeback_service.py`。变更首次/增量 prompt 构造选择和参数合同时，优先扩展 `tests/test_run_prompt_projection.py`，再按影响范围扩展 `tests/test_prompt_templates.py`、`tests/test_plan_run_flow.py` 或 `tests/test_implementation_session_flow.py`。变更 wiki context、confirmed plan / implementation context、assembled context、run instructions、execution policy、context index 文件写入或 `execution-policy.json` 读取时，优先扩展 `tests/test_run_context_artifact_service.py`，再按影响范围扩展 `tests/test_prompt_templates.py`、`tests/test_plan_run_flow.py`、`tests/test_implementation_session_flow.py`、`tests/test_status_reconcile_flow.py` 或 `tests/test_qa_flow.py`。变更 start_run 或 active run reconcile 的 artifact / agent_run / merge-test record 写回 payload 聚合时，优先扩展 `tests/test_run_ledger_projection.py`，再按影响范围扩展 `tests/test_implementation_session_flow.py`、`tests/test_qa_flow.py`、`tests/test_merge_test_basic_flow.py` 或 `tests/test_status_reconcile_flow.py`；实际 ledger append/upsert callback 调用归属 `tests/test_run_ledger_writeback_service.py` / `run/services/run_ledger_writeback_service.py`。变更 observed run report、stale completion 观测、execution policy decision 读取、run-level diff guard violations 组合、verification limitations fallback projection、completion report payload、agent run record 构造、reconciled agent run record 构造、reconcile result payload 构造、existing run mode/changed files 规则、merge-test run record 构造、project writeback payload 构造、start_run result payload 构造、run completion 状态/phase/report 投影或 run orchestration helper wrapper 时，优先扩展 `tests/test_run_orchestration_service.py`；start_run 观测、blocked/refinement wrapper、execution policy decision 读取、run-level diff guard violations 组合规则和 verification limitations fallback projection 优先扩展 `tests/test_run_orchestration_start_rules.py`，existing run reconcile 规则优先扩展 `tests/test_run_orchestration_reconcile_rules.py`，再按影响范围扩展 `tests/test_bugfix_writeback_flow.py`、`tests/test_plan_run_flow.py`、`tests/test_command_run_flow.py`、`tests/test_status_reconcile_flow.py`、`tests/test_qa_flow.py` 或 `tests/test_merge_test_qa_gate_flow.py`。
- 变更 completed run 或 active run reconcile 的 run summary writer payload 聚合时，优先扩展 `tests/test_run_summary_projection.py`，再按影响范围扩展 `tests/test_plan_run_flow.py` 或 `tests/test_status_reconcile_flow.py`；summary artifact 读取归属 `run_summary_artifact_service.py`，summary writer host callback 归属 `run/services/run_summary_writeback_service.py`，实际 LLM Wiki 写入仍由注入的 `RunSummaryWriter` / `KnowledgePort` 承担。
- 变更 completed run 或 active run reconcile 的 summary writer callback 调用边界时，优先扩展 `tests/test_run_summary_writeback_service.py`，再按影响范围扩展 `tests/test_plan_run_flow.py` 或 `tests/test_status_reconcile_flow.py`；该 service 不得读取 artifact、不写 ledger、不直接 import `RunSummaryWriter` / `KnowledgePort`。
- 变更 fresh run_dir 或 existing run record 的 `ArtifactSet` 路径合同时，优先扩展 `tests/test_run_artifact_paths.py`，再按影响范围扩展 `tests/test_status_reconcile_flow.py`、`tests/test_run_report_artifact_service.py`、`tests/test_run_summary_artifact_service.py` 或 `tests/test_plan_run_flow.py`；artifact 文件读写由对应 artifact service 承担，ledger append/upsert callback 由 `run/services/run_ledger_writeback_service.py` 消费 records 并调用注入 ledger callback，状态推进仍由 orchestrator host 边界承担。
- 变更 completed run 的 Project writeback stale gate、payload 到 WorkItemService callback 的 host 调用边界时，优先扩展 `tests/test_run_project_writeback_service.py`，再按影响范围扩展 `tests/test_bugfix_writeback_flow.py`、`tests/test_run_orchestration_service.py` 或 `tests/test_plan_run_flow.py`；实际 WorkItemService 业务写回、MCP transport 和 ledger `task_session.project_writeback` 更新仍由 `WorkItemService` / orchestrator host wrapper 承担。
- 变更 gateway 命令组、`/coding commands`、doctor 拦截或 coding group dispatch 时，优先扩展 `tests/test_gateway_command_group_flow.py`。
- 变更 Gateway event source、binding key、active task、coding mode、active project、pending rewrite/action 或 stale binding cleanup 时，优先扩展 `tests/test_gateway_binding_service.py`，再按影响范围扩展 gateway flow 测试。
- 变更显式 `/coding` / `/commands` parsing、`/coding` command normalization、project subcommand 映射、命令 route plan、handler key、reply mode、task id 来源策略、merge-test flag/task id 解析、rewrite canonical command、确认/取消回复分类、rewrite confirmation gate、plugin-generated message 过滤、Gateway event dedupe 或授权探测时，优先扩展 `tests/test_gateway_command_controller.py`，再按影响范围扩展 gateway flow 测试。
- 变更 Gateway custom route 分发、task/run/delivery/implementation/QA/prepare/merge-test handler selection 或 route metadata 到 host action 的映射时，优先扩展 `tests/test_gateway_command_executor.py`，再按影响范围扩展 `tests/test_gateway_command_group_flow.py`、`tests/test_command_run_flow.py`、`tests/test_merge_test_basic_flow.py`、`tests/test_merge_test_blocked_flow.py` 或 `tests/test_merge_test_qa_gate_flow.py`。
- Task 29 已完成：Gateway command controller 瘦身只覆盖 `gateway_command_controller.py` 纯解析/route metadata、`gateway_command_executor.py` custom route host shell、`gateway_pending_action_executor.py` pending action host shell 和 `gateway_active_context.py` active project 回填；runner/workspace/git/run lifecycle 不再作为 Task 29 后续项。
- 变更 Gateway coding mode enter/exit、pending rewrite 确认/取消、高置信度 rewrite direct execute、低置信度 handoff 或 rewrite context 装配时，优先扩展 `tests/test_gateway_coding_mode_executor.py`，再按影响范围扩展 `tests/test_gateway_rewrite_flow.py`、`tests/test_gateway_natural_language_command_flow.py` 或 `tests/test_gateway_pending_confirmation_flow.py`。
- 变更 Gateway pending action 确认/取消、latest human_required merge-test fallback、取消任务 gate 或确认后显式命令续接时，优先扩展 `tests/test_gateway_pending_action_executor.py`，再按影响范围扩展 `tests/test_gateway_pending_confirmation_flow.py`、`tests/test_cancel_restore_flow.py`、`tests/test_merge_test_blocked_flow.py`、`tests/test_merge_test_qa_gate_flow.py` 或 `tests/test_gateway_rewrite_flow.py`。
- 变更 implementation workspace 复用/创建、source branch/base branch、QA artifact 收集、clean-tree checkpoint、git HEAD 或 diff guard QA artifact 过滤时，优先扩展 `tests/test_workspace_checkpoint_service.py`，再按影响范围扩展 `tests/test_implementation_workspace_flow.py`、`tests/test_qa_flow.py` 或 `tests/test_merge_test_qa_gate_flow.py`。
- 变更 runner session metadata 到 run manifest 的 host callback 接线时，优先扩展 `tests/test_run_manifest_session_writeback_service.py`，再按影响范围扩展 `tests/test_run_manifest_service.py`、`tests/test_implementation_session_flow.py` 或 `tests/test_run_orchestration_start_rules.py`。
- 变更 fresh completed run 的完成态写回协调时，优先扩展 `tests/test_run_completion_writeback_service.py`，再按影响范围扩展 run ledger/session/summary/project writeback tests 和 plan/implementation/QA/merge-test 主流程 tests。
- 变更 active run reconcile 的完成态写回协调时，优先扩展 `tests/test_run_reconcile_writeback_service.py`，再按影响范围扩展 `tests/test_status_reconcile_flow.py`、run status/ledger/session/summary writeback tests 和 plan/implementation/QA/merge-test 主流程 tests；该 coordinator 不承担 runner dispatch、diff guard、QA evidence、dirty-check、checkpoint、manifest session metadata 或 Project writeback。
- 变更 run-manifest 基础字段、启动期 manifest update projection、artifact record、Codex attach/resume 展示命令、manifest session metadata 字段投影、controlled bypass 权限 profile 或 source elevated plan 权限判断时，优先扩展 `tests/test_run_manifest_service.py`，再按影响范围扩展 `tests/test_implementation_session_flow.py`、`tests/test_plan_run_flow.py`、`tests/test_source_plan_flow.py`、`tests/test_qa_flow.py` 或 `tests/test_merge_test_basic_flow.py`。
- 变更项目上下文、active project、项目目录识别、Feishu escaped project slug 或项目澄清时，优先扩展 `tests/test_gateway_project_task_flow.py`。
- 变更 project profile 读取、registry fallback、别名查找、动态来源计数或 project list/status 格式化时，优先扩展 `tests/test_project_profile_catalog.py`，再按影响范围扩展 `tests/test_gateway_project_task_flow.py`、`tests/test_gateway_rewrite_flow.py` 或 `tests/test_gateway_binding_service.py`。
- 变更 active project 应用到 task、project context 回填或对应 human decision 记录时，优先扩展 `tests/test_gateway_active_context.py`，再按影响范围扩展 `tests/test_gateway_project_task_flow.py`、`tests/test_gateway_natural_language_command_flow.py` 或 `tests/test_command_run_flow.py`。
- 变更 Coding Mode rewrite context 投影、active/known task 白名单字段、media type 投影或 task next-step hint 规则时，优先扩展 `tests/test_gateway_rewrite_context.py`，再按影响范围扩展 `tests/test_gateway_rewrite_flow.py` 或 `tests/test_gateway_natural_language_command_flow.py`。
- 变更计划确认、确认过早、`/coding use`、`/coding delete` 或近期 planned task continue 行为时，优先扩展 `tests/test_gateway_task_control_flow.py`。
- 变更 bugfix/change/continue feedback、带图反馈或 failed plan-only restart 行为时，优先扩展 `tests/test_gateway_feedback_flow.py` 和 `tests/test_gateway_change_continue_flow.py`。
- 变更 plugin-generated message 忽略、stale run completion 或无任务强确认拦截时，优先扩展 `tests/test_gateway_safety_lifecycle_flow.py`。
- 变更普通 `/coding list` 或 Gateway list host shell 的 active status 查询、active task 标记、空列表文案或会话切换提示时，优先扩展 `tests/test_coding_task_list_command_executor.py`，再按影响范围扩展 `tests/test_task_list_presenter.py`、`tests/test_completion_flow.py` 或 `tests/test_gateway_natural_language_command_flow.py`。
- 变更普通 `/coding help`、Gateway `/commands` listing、分页或 Hermes built-in command list 拼接时，优先扩展 `tests/test_coding_help_command_executor.py`，再按影响范围扩展 `tests/test_command_catalog.py`、`tests/test_gateway_command_group_flow.py` 或 `tests/test_gateway_coding_mode_lifecycle_flow.py`。
- 变更普通 `/coding status` 或 Gateway status host shell 的参数解析、active task fallback、delivery/tree flag 分流或 status command façade 时，优先扩展 `tests/test_coding_status_command_executor.py`，再按影响范围扩展 `tests/test_status_reconcile_flow.py`、`tests/test_delivery_status_reconcile_flow.py` 或 `tests/test_gateway_command_group_flow.py`。
- 变更普通 `/coding run`、`/coding implement` 或 `/coding qa` 同步 command-mode host shell 的 task gate、human decision、QA request 或 `start_run()` 调用接线时，优先扩展 `tests/test_coding_run_command_executor.py`，再按影响范围扩展 `tests/test_command_run_flow.py`、`tests/test_qa_flow.py`、`tests/test_cancel_restore_flow.py` 或 `tests/test_delivery_command_executor.py`。`/coding run <parent> --next` 仍优先扩展 `tests/test_delivery_command_executor.py`。
- 变更普通 `prepare-merge-test` / `merge-test` command host shell、blocked risk release 接线或 QA 风险确认时，优先扩展 `tests/test_coding_merge_test_command_executor.py`，再按影响范围扩展 `tests/test_merge_test_basic_flow.py`、`tests/test_merge_test_blocked_flow.py`、`tests/test_merge_test_qa_gate_flow.py` 或 `tests/test_merge_test_readiness_flow.py`；用户可见 merge-test 文案仍归 `coding_orchestration/presenters/merge_test_presenter.py`，不要恢复 `CodingOrchestrator` 私有文案代理。
- 变更普通 `/coding continue/change/bugfix` 命令模式占位、Gateway active task feedback 分流、feedback human decision / draft knowledge 记录 helper 或 bugfix replan 选择规则时，优先扩展 `tests/test_coding_feedback_command_executor.py`，再按影响范围扩展 `tests/test_gateway_feedback_flow.py`、`tests/test_gateway_change_continue_flow.py`、`tests/test_gateway_task_control_flow.py` 或 `tests/test_gateway_natural_language_command_flow.py`。
- 变更普通 `/coding use/exit/cancel/restore/delete/complete` command-mode host shell 或 Gateway use/exit task binding shell 时，优先扩展 `tests/test_coding_task_control_command_executor.py`，再按影响范围扩展 `tests/test_cancel_restore_flow.py`、`tests/test_completion_flow.py`、`tests/test_gateway_task_control_flow.py` 或 `tests/test_gateway_project_task_flow.py`。
- 变更 Kanban create/status sync host helper、skipped/failed sync record 或 task status view 字段投影时，优先扩展 `tests/test_kanban_sync_service.py`，再按影响范围扩展 `tests/test_kanban_bridge.py`、`tests/test_orchestrator_run_flow.py`、`tests/test_run_status_transition_service.py` 或 `tests/test_task_status_payload.py`。
- 变更 active coding status 列表、取消态保护文案、cancelled task 恢复状态选择或 merged_test bugfix reopen 时，优先扩展 `tests/test_task_lifecycle_guard_service.py`，再按影响范围扩展 `tests/test_cancel_restore_flow.py`、`tests/test_coding_task_control_command_executor.py`、`tests/test_coding_feedback_command_executor.py` 或 `tests/test_gateway_command_executor.py`。
- 变更状态展示、active background run reconcile、implementation not landed reconcile 或 execution policy 写入时，优先扩展 `tests/test_status_reconcile_flow.py`。
- 变更 bugfix 完成后的飞书 Project comment/writeback 时，优先扩展 `tests/test_bugfix_writeback_flow.py` 和 `tests/test_workitem_service.py`。
- 变更手动 QA、QA artifact 收集、QA clean-tree gate 或实现完成后的 QA 提示时，优先扩展 `tests/test_qa_flow.py` 和相邻 `RunService` / prompt contract tests。
- 变更 prepare/merge-test 状态提示、blocked/QA 风险确认、风险放行说明或 merge-test 启动 ACK 文案时，优先扩展 `tests/test_merge_test_presenter.py`，再按影响范围扩展 `tests/test_merge_test_basic_flow.py`、`tests/test_merge_test_blocked_flow.py`、`tests/test_merge_test_qa_gate_flow.py` 或 `tests/test_gateway_natural_language_command_flow.py`。
- 变更 blocked merge-test readiness 纯评估时，优先扩展 `tests/test_merge_test_readiness_service.py`；变更 merge-test 手动触发、blocked readiness host flow、QA 风险 gate 或 merge-test 前 clean-tree gate 的状态/策略逻辑时，优先扩展 `tests/test_merge_test_basic_flow.py`、`tests/test_merge_test_readiness_flow.py`、`tests/test_merge_test_blocked_flow.py`、`tests/test_merge_test_qa_gate_flow.py` 和相邻状态策略测试。
- 变更 merged-test 后人工完成、`/coding list` 展示或 complete/list 文案时，优先扩展 `tests/test_completion_flow.py` 和相邻状态策略测试。
- 变更 plan-only/implementation/QA 启动 ACK、active run 重复启动提示或 cannot-start 恢复提示时，优先扩展 `tests/test_run_start_presenter.py`，再按影响范围扩展 `tests/test_command_run_flow.py`、`tests/test_plan_run_flow.py`、`tests/test_qa_flow.py` 或 `tests/test_run_service.py`。
- 变更 `/coding continue/change/bugfix` 反馈、需求变更、图片未捕获或人工澄清用户可见文案时，优先扩展 `tests/test_feedback_presenter.py`，再按影响范围扩展 `tests/test_coding_feedback_command_executor.py`、`tests/test_gateway_feedback_flow.py`、`tests/test_gateway_change_continue_flow.py`、`tests/test_gateway_task_control_flow.py` 或 `tests/test_gateway_natural_language_command_flow.py`；`coding_feedback_command_executor.py` 直接消费 `coding_orchestration/presenters/feedback_presenter.py`，不要恢复 `CodingOrchestrator._*feedback*` 或 `_human_clarification_*` 私有代理。
- 变更 Coding Mode rewrite 确认、低置信度补充或 handoff 用户可见文案时，优先扩展 `tests/test_gateway_rewrite_presenter.py`，再按影响范围扩展 `tests/test_gateway_rewrite_flow.py`、`tests/test_gateway_pending_confirmation_flow.py` 或 `tests/test_gateway_natural_language_command_flow.py`。`gateway_coding_mode_executor.py` 直接消费 `gateway_rewrite_presenter.py`，不要恢复 `CodingOrchestrator._rewrite_confirmation_message`、`_rewrite_needs_human_confirmation_message`、`_rewrite_rejection_user_text` 或 `_rewrite_handoff_to_hermes_message` 私有代理。
- 变更 `/coding list` 任务摘要、项目/描述标签、`/coding status` 详情、Kanban/完成回传/QA 缺口展示或 run completion 用户可见消息时，优先扩展 `tests/test_task_list_presenter.py`、`tests/test_task_status_presenter.py`、`tests/test_run_completion_presenter.py`，再按影响范围扩展 `tests/test_status_reconcile_flow.py`、`tests/test_completion_flow.py`、`tests/test_plan_run_flow.py`、`tests/test_implementation_result_flow.py` 或 `tests/test_gateway_safety_lifecycle_flow.py`。`coding_task_list_command_executor.py` 和 `gateway_coding_mode_executor.py` 直接消费 `coding_orchestration/presenters/task_list_presenter.py`，不要恢复 `CodingOrchestrator._format_task_list`、`_task_project_label` 或 `_task_description_label` 私有代理；`coding_status_command_executor.py` 直接消费 `coding_orchestration/presenters/task_status_presenter.py`，不要恢复 `CodingOrchestrator._format_task_status_details`、status display、latest QA 或 QA health 私有代理。
- 变更普通 `/coding ...` command façade 分发表、command-level wrapper、delivery rollup/materialize helper、artifact purge callback 或 prepare-merge-test status callback 时，优先扩展 `tests/test_architecture_guard.py`、对应 command executor 测试和 `tests/test_command_run_flow.py`；不要把这些实现恢复到 `orchestrator.py` 主文件。
- 变更 tool operation façade、`tool_*` 入口、tool run/source preflight adapter 或 Project MCP / WorkItem 小包装时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_tool_operation_dispatcher.py`、`tests/test_orchestrator_tools.py`、`tests/test_coding_cli.py` 和 `tests/test_workitem_service.py`；不要把这些实现恢复到 `orchestrator.py` 主文件。
- 变更 bootstrap façade、`__post_init__`、`set_dispatch_tool`、`from_default_config` 或默认运行根 helper 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_orchestrator_run_flow.py`、`tests/test_gateway_trigger.py`、`tests/test_gateway_command_group_flow.py`、`tests/test_coding_cli.py` 和 `tests/test_plugin_registration.py`；不要把这些 wrapper 恢复到 `orchestrator.py` 主文件。
- 变更 diagnostics / dashboard façade、doctor/CLI wrapper、Meegle preflight fallback、dashboard status payload 或 preflight/source-resolve formatter wrapper 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_coding_diagnostics_command_executor.py`、`tests/test_coding_cli.py`、`tests/test_dashboard_api_contract.py`、`tests/test_gateway_command_group_flow.py` 和 `tests/test_source_resolver.py`；不要把这些实现恢复到 `orchestrator.py` 主文件。
- 变更 Gateway route façade、immediate/custom route dispatch、Coding Mode rewrite wrapper、pending action/rewrite binding wrapper、确认/取消词 wrapper、commands listing route、dedupe/授权/looks-like-task wrapper 时，优先扩展 `tests/test_architecture_guard.py` 和 Gateway route/rewrite/pending 相邻 flow 测试；不要把这些实现恢复到 `orchestrator.py` 主文件。
- 变更 project/profile façade、project profile catalog wrapper、active project binding wrapper、本地项目候选/路径/别名 wrapper、active project 回填 wrapper、人工项目画像 upsert 或项目澄清 host callback 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_project_profile_catalog.py`、`tests/test_gateway_project_context.py`、`tests/test_gateway_active_context.py`、`tests/test_project_command_executor.py`、`tests/test_gateway_project_task_flow.py` 和相邻 Gateway rewrite/feedback/source context 测试；不要把这些实现恢复到 `orchestrator.py` 主文件。
- 变更 task/source façade、task create command wrapper、TaskService source/status payload wrapper、source context 读取/修复/enrich wrapper、event/media ledger helper 或 draft knowledge source refs wrapper 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_task_service.py`、`tests/test_task_status_payload.py`、`tests/test_source_context_repair_service.py`、`tests/test_source_flow.py`、`tests/test_source_plan_flow.py` 和相邻 Gateway feedback/command 测试；不要把这些实现恢复到 `orchestrator.py` 主文件。
- 变更 task runtime façade、Kanban/status host wrapper、task list/status event wrapper、feedback active-task wrapper、active task/coding mode binding wrapper、task lifecycle wrapper、人工决策记录或 RunService gate wrapper 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_kanban_sync_service.py`、`tests/test_task_lifecycle_guard_service.py`、`tests/test_coding_feedback_command_executor.py`、`tests/test_coding_status_command_executor.py`、`tests/test_coding_task_list_command_executor.py`、`tests/test_gateway_coding_mode_executor.py`、`tests/test_gateway_command_executor.py`、`tests/test_cancel_restore_flow.py`、`tests/test_command_run_flow.py` 和 `tests/test_status_reconcile_flow.py`；不要把这些 wrapper 恢复到 `orchestrator.py` 主文件。
- 变更 workspace façade、implementation/merge-test workspace 查找、QA artifact 收集、QA/merge-test checkpoint 准备、dirty/head/clean checkpoint 观测、source branch/base branch 投影、task short id/slug 或 latest existing implementation workspace wrapper 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_workspace_checkpoint_service.py`、`tests/test_implementation_workspace_flow.py`、`tests/test_implementation_session_flow.py`、`tests/test_qa_flow.py`、`tests/test_merge_test_basic_flow.py`、`tests/test_merge_test_qa_gate_flow.py`、`tests/test_run_checkpoint_preparation_service.py`、`tests/test_run_evidence_observation_service.py`、`tests/test_command_run_flow.py` 和 `tests/test_status_reconcile_flow.py`；不要把这些 wrapper 恢复到 `orchestrator.py` 主文件。
- 变更 background façade、后台 plan/implementation/QA/merge-test 启动与通知 wrapper、completion wait/failed/pending-action/notification wrapper、sender scheduling/reply wrapper 或 execution policy artifact wrapper 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_coding_background_run_executor.py`、`tests/test_background_run_notifier.py`、`tests/test_run_background_orchestration.py`、`tests/test_gateway_command_executor.py`、`tests/test_gateway_coding_mode_executor.py`、`tests/test_gateway_pending_action_executor.py`、`tests/test_plan_run_flow.py`、`tests/test_qa_flow.py`、`tests/test_merge_test_qa_gate_flow.py`、`tests/test_command_run_flow.py` 和 `tests/test_run_context_artifact_service.py`；不要把这些 wrapper 恢复到 `orchestrator.py` 主文件。
- 变更 status policy façade、report status details、implementation status normalize、verification limitations 补齐或 run result 到 task status/phase 映射 wrapper 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_status_policy.py`、`tests/test_run_service.py`、`tests/test_status_reconcile_flow.py`、`tests/test_run_orchestration_start_rules.py` 和 `tests/test_task_lifecycle_guard_service.py`；不要把这些 wrapper 恢复到 `orchestrator.py` 主文件。
- 变更 manifest façade、existing/fresh artifact set、run manifest 构造、Codex attach/resume 命令、session id 读取、runner manifest name 或 manifest session metadata 写回 wrapper 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_run_manifest_service.py`、`tests/test_run_manifest_session_writeback_service.py`、`tests/test_run_start_artifact_service.py`、`tests/test_run_manifest_artifact_service.py`、`tests/test_run_orchestration_start_rules.py`、`tests/test_implementation_session_flow.py`、`tests/test_plan_run_flow.py` 和 `tests/test_status_reconcile_flow.py`；不要把这些 wrapper 恢复到 `orchestrator.py` 主文件。
- 变更 prompt/context façade、resumed session 增量上下文、wiki docs/refs、context artifacts、dependency/sibling task context、confirmed plan / merge-test context、report summary markdown 或 wiki ref 投影 wrapper 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_run_context_artifact_service.py`、`tests/test_run_report_artifact_service.py`、`tests/test_context_assembler.py`、`tests/test_plan_run_flow.py`、`tests/test_implementation_session_flow.py` 和 `tests/test_ledger_wiki_orchestrator.py`；不要把这些 wrapper 恢复到 `orchestrator.py` 主文件。
- 变更 merge-test façade、merge-test blocker、blocked task 风险放行 host 写回、blocked assessment fact collection、latest implementation run/source branch/disallowed reason wrapper 或 QA evidence wrapper 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_merge_test_readiness_service.py`、`tests/test_merge_test_readiness_flow.py`、`tests/test_merge_test_blocked_flow.py`、`tests/test_merge_test_qa_gate_flow.py`、`tests/test_merge_test_basic_flow.py`、`tests/test_coding_merge_test_command_executor.py` 和 `tests/test_gateway_command_executor.py`；不要把这些 wrapper 恢复到 `orchestrator.py` 主文件。
- 变更 runtime/failure façade、flag helper、project/workflow wrapper、plan report session/execution policy/timeout wrapper、runner failed result 或 checkpoint failed result 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_run_service.py`、`tests/test_run_dispatch_service.py`、`tests/test_run_orchestration_start_rules.py`、`tests/test_run_orchestration_plan_report_session.py`、`tests/test_run_session_projection.py`、`tests/test_command_run_flow.py`、`tests/test_status_reconcile_flow.py` 和 `tests/test_task_service.py`；不要把这些 wrapper 恢复到 `orchestrator.py` 主文件。
- 变更 active run façade、completed active run reconcile host shell 或 agent run lookup helper 时，优先扩展 `tests/test_architecture_guard.py`、`tests/test_status_reconcile_flow.py`、`tests/test_run_reconcile_writeback_service.py`、`tests/test_run_orchestration_reconcile_rules.py`、`tests/test_run_report_artifact_service.py`、`tests/test_run_summary_artifact_service.py`、`tests/test_run_orchestration_service.py` 和 `tests/test_command_run_flow.py`；不要把这些 wrapper 恢复到 `orchestrator.py` 主文件。
- 变更任务取消、误取消恢复、已取消任务继续/变更/bugfix 拒绝或取消确认文案时，优先扩展 `tests/test_cancel_restore_flow.py` 和相邻状态机测试。
- 变更 skill core 或 Hermes binding 时优先扩展 `tests/test_plugin_registration.py`，确保 core 不含 host 细节、Hermes binding 仍注册且只做映射。
- 变更 `orchestrator.py` 时先按行为域查找相邻 flow 小文件；只有端到端 smoke、kanban transition、plan-only resume sandbox 或无法归类的 façade 行为才扩展 `tests/test_orchestrator_run_flow.py`，工具层扩展 `tests/test_orchestrator_tools.py`，Gateway 触发入口扩展 `tests/test_gateway_trigger.py`。
- 变更安装链路时优先查找并扩展 `tests/test_install.py`、`tests/test_docs_and_install_entry.py`。
- 变更 report contract 或 runner 输出时优先查找并扩展 `tests/test_report_contract.py`、`tests/test_report_admission.py`、`tests/test_codex_report*.py`、`tests/test_codex_cli_report_facade.py` 和 `tests/test_codex_cli_report_failure_facade.py`；变更 Codex command/process façade 时扩展 `tests/test_codex_cli_command_facade.py` 或 `tests/test_codex_cli_process_facade.py`。

## 发布与运行约束

- 本项目当前只支持本地软链接安装到 Hermes：`~/.hermes/plugins/coding_orchestration -> 当前仓库/coding_orchestration`。
- 插件更新后必须重启 Hermes Gateway；Gateway 不会热加载 Python 插件代码。
- `implementation`、`QA`、`merge-test` 可使用受控高权限 Codex CLI session，但源码修改应落在任务 workspace；项目外写入只允许依赖缓存、git metadata、dev server/browser 临时文件和 QA artifact。
- `plan-only` 无论是否需要读取外部来源，都不应修改项目文件；diff guard 应阻断 plan 阶段写入。
- `merge-test` 和发布仍是人工触发，不由普通 implementation 自动发布或部署。

## 文档约定

- `AGENTS.md` 只做导航和 hard stops。
- `docs/project-map.md`、`docs/conventions.md`、`docs/component-contract.md` 是人类可读项目事实入口。
- `contracts/project-context.yaml` 是 machine-readable 项目事实，不承载 agent topology、角色拆分或执行顺序。
- `docs/deployment.md`、`docs/plugin-prerequisites.md`、`docs/plans/`、状态机/交付流文档属于项目沉淀，可以提交。
- superpowers 生成的执行计划统一放在 `docs/plans/`，不要再分散到 `docs/superpowers/`、`docs/local/superpowers*` 或 `docs/` 根目录。
- 宣讲、demo、分享材料放在 `docs/local/presentations/`；该目录被忽略，不作为 canonical 项目事实来源。
- 历史计划、流程图和阶段性报告不要复制进 canonical 文档；只在需要背景时按链接读取。
