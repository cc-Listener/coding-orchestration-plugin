# 项目地图

本仓库是 `coding_orchestration` Hermes 用户插件源码仓库。它把飞书 `/coding` 需求入口、Hermes Gateway、Task Ledger、LLM Wiki 和 Codex CLI runner 组织成一个可审计的编码任务闭环。

## 仓库形态

- 仓库类型：Python Hermes plugin。
- 工作区形态：单包源码仓库，没有 `apps/*` 或独立主前端子域。
- 运行入口：Hermes 通过 `~/.hermes/plugins/coding_orchestration -> 当前仓库/coding_orchestration` 软链接加载插件。
- 运行根：默认固定为 `~/.hermes/coding-orchestration`，用于 ledger、runs、workspaces 和本地 LLM Wiki。

## 顶层结构

| 路径 | 角色 |
| --- | --- |
| `coding_orchestration/__init__.py` | Hermes plugin 注册入口，注册 Gateway hook、`/coding` 命令、native tools 和 Hermes binding skill；core skill 作为合同文件存在，不直接注册为 Hermes skill。 |
| `coding_orchestration/orchestrator.py` | 编排 façade：处理 Gateway 事件入口、状态回写和剩余 run lifecycle 接线；任务、运行策略、后台通知、run orchestration 第一切片、会话绑定、gateway command 纯规则与 route plan、workspace/checkpoint、run manifest/session policy、runner dispatch、run artifact 路径/写回、run diff guard observation、run session/ledger writeback callback、completion presentation 与工作项用例逐步委托给 services / notifier / presenter / run orchestration helper / artifact path projection / artifact service / dispatch service / writeback service / binding / controller / executor / support service / façade mixin。当前 active run reconcile 只保留读取/观测前置逻辑，完成态写回 tail 已委托 coordinator。 |
| `coding_orchestration/orchestrator_command_facade.py` | Orchestrator command façade mixin：承接普通 `/coding ...` command façade 分发表、command-level wrapper、delivery rollup/materialize helper、artifact purge callback 和 prepare-merge-test status callback；`CodingOrchestrator` 只继承 mixin 保留兼容方法名，不承载 Gateway route、tool operation dispatcher、task/source façade、runner/workspace/git、`start_run()` 或 run lifecycle。 |
| `coding_orchestration/orchestrator_tool_facade.py` | Orchestrator tool façade mixin：承接 tool operation dispatcher façade、`tool_*` 入口、tool run/source preflight adapter 和 Project MCP / WorkItem 小包装；`CodingOrchestrator` 只继承 mixin 保留兼容方法名，不承载 task/source façade、Gateway route、普通 command façade、runner/workspace/git、`start_run()` 或 run lifecycle。 |
| `coding_orchestration/orchestrator_diagnostics_facade.py` | Orchestrator diagnostics façade mixin：承接 doctor/CLI wrapper、Meegle preflight fallback、dashboard status payload 和 preflight/source-resolve formatter wrapper；`CodingOrchestrator` 只继承 mixin 保留兼容方法名，不承载 Gateway route、tool operation façade、task/source façade、runner/workspace/git、`start_run()` 或 run lifecycle。 |
| `coding_orchestration/orchestrator_gateway_facade.py` | Orchestrator Gateway façade mixin：承接 Gateway immediate/custom route dispatch、Coding Mode rewrite wrapper、pending action/rewrite binding wrapper、确认/取消词 wrapper、commands listing route、dedupe/授权/looks-like-task wrapper；`CodingOrchestrator` 只继承 mixin 保留兼容方法名，不承载 `handle_gateway_event()` 入口、task/source façade、active task/project binding 业务、runner/workspace/git、`start_run()` 或 run lifecycle。 |
| `coding_orchestration/orchestrator_project_facade.py` | Orchestrator project façade mixin：承接 project profile catalog wrapper、active project binding wrapper、本地项目候选/路径/别名 wrapper、active project 回填 wrapper、人工项目画像 upsert 和项目澄清 host callback；`CodingOrchestrator` 只继承 mixin 保留兼容方法名，不承载 task/source façade、Gateway route、active task/coding mode binding、runner/workspace/git、`start_run()` 或 run lifecycle。 |
| `coding_orchestration/orchestrator_task_source_facade.py` | Orchestrator task/source façade mixin：承接 task create command wrapper、TaskService source/status payload wrapper、source context 读取/修复/enrich wrapper、event/media ledger helper 和 draft knowledge source refs wrapper；`CodingOrchestrator` 只继承 mixin 保留兼容方法名，不承载 active task/coding mode binding、feedback command host shell、runner/workspace/git、`start_run()` 或 run lifecycle。 |
| `coding_orchestration/orchestrator_task_runtime_facade.py` | Orchestrator task runtime façade mixin：承接 Kanban/status host wrapper、task list/status event wrapper、feedback active-task wrapper、active task/coding mode binding wrapper、task lifecycle wrapper、人工决策记录和 RunService gate wrapper；`CodingOrchestrator` 只继承 mixin 保留兼容方法名，不承载 `handle_gateway_event()`、active run reconcile、runner/workspace/git、`start_run()` 或 run lifecycle。 |
| `coding_orchestration/gateway_command_controller.py` | Gateway `/coding` command controller 纯规则：显式 `/coding` / `/commands` 解析、命令归一化、命令 route plan、handler key、reply mode、task id 来源策略、diagnostic immediate reply 路由、merge-test 参数解析、rewrite canonical command、确认/取消词分类、rewrite 风险确认、plugin echo 过滤、Gateway event dedupe key 和授权探测；不承载 ledger、runner、消息发送或状态推进。Task 29 完成边界要求继续把 controller 视为纯规则层。 |
| `coding_orchestration/gateway_command_executor.py` | Gateway `/coding` custom route host shell：消费 controller route metadata，承接 task/run/delivery/implementation/QA/prepare/merge-test 等复杂命令分发；run start ACK / active-run 文案 direct 调用 `run_start_presenter.py`，merge-test 风险确认、QA 风险确认、启动 ACK 和放行说明 direct 调用 `merge_test_presenter.py`，其余副作用通过 orchestrator façade/callback 触发 ledger、runner、pending action 和消息回复，迁移期不承载纯解析规则。Task 29 完成后，runner/workspace/git/run lifecycle 不回挂到该 executor。 |
| `coding_orchestration/coding_help_command_executor.py` | Coding help command host shell：维护普通 `/coding help`、Gateway `/commands` listing、分页和 Hermes built-in command list 拼接；不承载 command catalog 纯数据、Gateway route/controller、runner/workspace/git 或 run lifecycle。 |
| `coding_orchestration/gateway_coding_mode_executor.py` | Gateway coding mode host shell：维护自然语言 coding mode enter/exit、pending rewrite 确认/取消、高置信度 rewrite direct execute、低置信度 handoff 和 rewrite context 装配；rewrite 确认/补充/handoff 文案 direct 调用 `gateway_rewrite_presenter.py`，active-run 重复确认文案 direct 调用 `run_start_presenter.py`，不承载 Gateway route/controller、pending action executor、task 创建实现、runner/workspace/git 或 run lifecycle。 |
| `coding_orchestration/gateway_pending_action_executor.py` | Gateway pending action host shell：处理待确认动作的确认/取消回复、latest human_required merge-test fallback、取消任务 gate 和显式命令续接；通过 orchestrator façade/callback 调用 binding、ledger 和消息回复，不承载 controller 纯解析规则。 |
| `coding_orchestration/gateway_active_context.py` | Gateway active context host helper：把会话级 active project 应用到缺少项目上下文的 task，并记录 human decision；不负责 active project binding 存取、项目初始化或项目画像生成。 |
| `coding_orchestration/gateway_project_context.py` | Gateway project context 纯规则 helper：识别人工输入中的本地项目文件夹候选、解析本地项目路径、合并 registry/注入搜索根并抽取人工别名；不写 project profile、不绑定 active project、不发送 Gateway 回复。 |
| `coding_orchestration/gateway_rewrite_context.py` | Gateway rewrite context 纯投影 helper：把已采集的 user text、active/known task、active/known project、media、command catalog 和 allowed commands 白名单投影成 LLM rewrite context，并计算 task next-step hint；不读取 ledger、不持有 Gateway event、不调用 command rewriter、不发送回复。 |
| `coding_orchestration/source_context_repair_service.py` | Source context repair host service：维护 source context 读取、deferred source pre-run enrichment、既有 task context 修复、source summary 回写和项目上下文回填接线；不改 legacy `source_context` schema、不实现 source reader、不承载 source 权限策略、runner/workspace/git 或 run lifecycle。 |
| `coding_orchestration/kanban_sync_service.py` | Kanban sync host service：维护 task 创建时的 Kanban create 写回、状态同步写回、skipped/failed sync record 和 task status view 字段投影；不改 `kanban_bridge.py` 工具映射、不改状态机、不承载 runner/workspace/git 或 run lifecycle。 |
| `coding_orchestration/task_lifecycle_guard_service.py` | Task lifecycle guard service：维护 active coding status 列表、取消态保护文案、cancelled task 恢复状态选择和 merged_test bugfix reopen 写回；不改状态机、不承载 runner/workspace/git 或 run lifecycle。 |
| `coding_orchestration/merge_test_readiness_service.py` | Merge-test readiness 纯评估 service：基于 blocked task、implementation run、workspace/source branch、Codex session 和 structured report 判断是否可风险放行 merge-test；只返回评估 dict，不写 ledger、不推进状态、不触发 runner、不发送 Gateway 回复。 |
| `coding_orchestration/project_profile_catalog.py` | Project profile catalog：读取 LLM Wiki project profile、合并 registry fallback、查找 name/alias/project/path basename、计算动态来源数量并格式化 project list/status；不写 profile、不绑定 active project、不发送 Gateway 回复。 |
| `coding_orchestration/project_command_executor.py` | Project command executor host shell：维护 `/coding project list/init/use/status/clear` 命令模式提示和 Gateway immediate project context 分支，调用 host 提供的项目路径解析、profile upsert、active project binding 与 project list/status formatter；不拥有 Gateway route、不创建任务、不启动 runner、不推进状态、不实现 project profile catalog 纯规则。 |
| `coding_orchestration/config.py` | 运行配置和工具默认值边界；用于逐步收口路径、命令、域名和 env key hard code。 |
| `coding_orchestration/tool_specs.py` | Hermes native tools / future MCP tools 的 host-agnostic 工具规格合同。 |
| `coding_orchestration/tool_operation_dispatcher.py` | `ToolOperationDispatcher`：按 `ToolSpec.operation_id` 分发到当前 service/façade handler；`orchestrator_tool_facade.py` 只负责 host façade 接线；`plugin_tools.py` Hermes native tool 注册层只做 host payload 归一和 handler 包装，不持有 orchestrator 方法名映射；`cli.py` 的 tool-equivalent 命令和 Gateway `/coding project-mcp-preflight` diagnostic leaf 复用同一 operation 边界。 |
| `coding_orchestration/cli.py` | Hermes CLI 子命令入口；纯 tool-equivalent 命令直接走 `dispatch_tool_operation()`，普通 `status <task_id>` 只读取 `task.status` payload，`project-mcp-preflight` 额外保留 host config 与 stdio command readiness gate。 |
| `coding_orchestration/ports.py` | 逐步拆分核心域和 adapter 的端口合同，覆盖 host、runner、source、work item、ledger、knowledge 和 runtime；`SourceResult` 是 source 读取的稳定结果合同。 |
| `coding_orchestration/models.py` | 公共枚举和数据结构：任务状态、phase、run mode、runner capability、artifact contract。 |
| `coding_orchestration/state_machine.py` | 任务状态转换规则，以及 runner/source 状态到 task 状态的映射。 |
| `coding_orchestration/status_policy.py` | 运行状态详情策略：report 状态归一化、known gaps、runner_failed、implementation_not_landed 和 verification limitations 判定。 |
| `coding_orchestration/doctor_presenter.py` | doctor、lark-preflight、project-mcp-preflight 和 source-resolve 的用户可见文案 presenter；承载 host 恢复命令和配置引用，`coding_diagnostics_command_executor.py` 负责状态收集并 direct 调用 presenter，不经 `CodingOrchestrator._doctor_*` 私有代理。 |
| `coding_orchestration/coding_task_list_command_executor.py` | Coding task list command host shell：维护普通 `/coding list` 与 Gateway list immediate 的 active status 查询、active task 标记、空列表文案和会话切换提示；不承载 task list presenter、Gateway route 规则、CLI direct dispatcher、status reconcile、delivery/project/diagnostics、runner/workspace/git 或 run lifecycle。 |
| `coding_orchestration/coding_status_command_executor.py` | Coding status command host shell：维护普通 `/coding status <task_id>` 与 Gateway status immediate 的参数解析、active task fallback、active run reconcile callback、delivery/tree status 委托和 status presenter 调用；不承载 active run reconcile 实现、delivery status executor、TaskStatus presenter、Gateway route 规则、CLI direct dispatcher、runner/workspace/git 或 run lifecycle。 |
| `coding_orchestration/coding_run_command_executor.py` | Coding run command host shell：维护普通 `/coding run <task_id>`、`/coding implement <task_id>` 和 `/coding qa <task_id>` 的同步 command-mode 分支，direct 调用 `run_start_presenter.py` / `run_completion_presenter.py` 渲染用户可见文案，并调用 host 的 task gate、human decision、QA request 和 `start_run()` callback；不承载 `/coding run <parent> --next`、Gateway 异步启动、`start_run()` 实现、active run reconcile、workspace/checkpoint/git、merge-test gates、delete/cancel/restore 或 run lifecycle。 |
| `coding_orchestration/coding_merge_test_command_executor.py` | Coding merge-test command host shell：维护普通 `prepare-merge-test` 与 `merge-test` 命令分支、blocked risk release 接线、QA 风险确认和 merge-test run 启动 callback；direct 调用 `merge_test_presenter.py` / `run_completion_presenter.py` 渲染用户可见文案，不承载 workspace/git/checkpoint 实现、blocked readiness 纯评估、QA evidence 观测、`start_run()` 实现或 run lifecycle。 |
| `coding_orchestration/coding_feedback_command_executor.py` | Coding feedback command host shell：维护普通 `/coding continue/change/bugfix` 命令模式占位、Gateway active task feedback 分流、feedback human decision / draft knowledge 记录 helper 和 bugfix replan 选择规则；direct 调用 `feedback_presenter.py` 渲染用户可见反馈文案，并通过 host callback 调用 Project clarification、后台 plan/implementation 启动和 merged-test reopen，不承载 Gateway route/controller、后台 runner 实现、`start_run()`、workspace/checkpoint/git、merge-test gates、source enrichment 或 Project MCP 写回。 |
| `coding_orchestration/coding_task_control_command_executor.py` | Coding task control command host shell：维护普通 `/coding use/exit/cancel/restore/delete/complete` command-mode 分支和 Gateway use/exit task binding shell；通过 host callback 调用 task status transition、artifact purge、binding 清理和 human decision 记录，不承载 Gateway route/controller、文件系统清理实现、runner/workspace/git、merge-test gates 或 run lifecycle。 |
| `coding_orchestration/gateway_rewrite_presenter.py`、`doctor_presenter.py`、`run_start_presenter.py`、`feedback_presenter.py`、`merge_test_presenter.py`、`task_list_presenter.py`、`task_status_presenter.py`、`run_completion_presenter.py` | 用户可见 presentation：Coding Mode rewrite 确认/补充/handoff 文案、doctor/preflight/source-resolve 摘要、plan-only/implementation/QA 启动 ACK、active run/cannot-start 提示、`/coding continue/change/bugfix` 反馈与人工澄清提示、prepare/merge-test 状态提示、blocked/QA 风险确认和放行说明、`/coding list` 任务摘要、`/coding status` 状态详情、Kanban/完成回传/QA 缺口展示、plan/implementation/QA/merge-test/stale run completion 消息、summary/risk/next_actions fallback；Gateway rewrite、doctor/preflight、run start、feedback、merge-test、task list、task status 与 run completion 文案由 coding mode / diagnostics / command / gateway / background executor 或 `RunService` 直接调用 presenter，orchestrator 不再保留 `_rewrite_*` / doctor / run start / feedback / merge-test / `_format_task_list` / `_task_*label` / `_format_task_status_details` / status display / latest QA / QA health / `_format_*completion_message` 私有代理；`_read_report_json` 暂作为跨模块 host callback 保留。 |
| `coding_orchestration/coding_background_run_executor.py` | Coding background run executor host shell：维护 Gateway 后台 plan-only / implementation / QA / merge-test mode-specific 启动、completion message 选择、失败通知和 completion notification 接线；不承载 `start_run()` 实现、后台等待完成、失败状态收敛、merge-test pending action、runner/workspace/git/checkpoint 或 run lifecycle。 |
| `coding_orchestration/background_run_notifier.py` | 后台 run 通知 host helper：后台线程启动、sender 调度、reply fallback、失败通知模板和 completion notification record；不承载 task 状态推进、runner 结果映射或 merge-test pending action。 |
| `coding_orchestration/run_background_orchestration.py` | 后台 run host orchestration helper：后台 queued/running 等待完成、后台启动失败状态收敛、merge-test `human_required` 转会话级 pending action；通过 orchestrator façade 调用 ledger/reconcile/report/binding，不承载 runner subprocess、Gateway 发送或 workspace/git 副作用。 |
| `coding_orchestration/run_failure_report_projection.py` | Run failure report projection helper：runner 启动异常、QA/merge-test checkpoint 失败的结构化 report payload、QA artifact 空对象和 tested commit 合同；不写 artifact、不包装 `RunResult`、不启动 runner。 |
| `coding_orchestration/run_report_refinement_projection.py` | Run report refinement projection helper：diff guard / implementation commit missing blocked report 构造、observed report status/details refinement 和 implementation commit dirty-check 信号；不执行 diff guard、不判断 workspace dirty、不写 report、不推进 ledger。 |
| `coding_orchestration/run_diff_guard_service.py` | Run diff guard observation host service：承接 run start 的 before snapshot、changed files 观测、QA report artifact 过滤、allowed/forbidden path violations、plan-only 写入 violations 补充和 diff summary 写回；只返回 `RunDiffGuardObservation`，不写 ledger/report/manifest/summary，不启动 runner 或推进状态。 |
| `coding_orchestration/run_dispatch_service.py` | Run dispatch host service：承接 run 启动期 checkpoint failure gate、`runner.run()` 调度和 runner exception fallback；只调用注入 runner 与失败 result callback，不写 ledger/report/manifest/summary，不执行状态 transition 或 diff guard。 |
| `coding_orchestration/run_status_transition_service.py` | Run status transition host service：承接 `_transition_task_status()` 的状态机/ledger/Kanban callback shell、run start/completion/reconcile transition 和 active run cleanup；通过注入 callback 工作，不直接持有 `TaskLedger`，不写 report/manifest/summary，不启动 runner，不执行 diff guard 或 QA evidence。 |
| `coding_orchestration/run_evidence_observation_service.py` | Run evidence observation host service：承接 QA artifact/tested commit observation 和 implementation dirty-check observation；只调用注入 callback 返回观测结果，不写 ledger/report/manifest/summary，不创建 checkpoint，不启动 runner 或推进状态。 |
| `coding_orchestration/run_checkpoint_preparation_service.py` | Run checkpoint preparation host service：承接 QA / merge-test checkpoint preparation callback 选择、调用和 manifest update payload 构造；只返回 update dict，不直接 mutate manifest，不写 artifact/ledger/report/summary，不启动 runner 或推进状态。 |
| `coding_orchestration/run_implementation_checkpoint_service.py` | Run implementation checkpoint writeback host service：承接 implementation dirty 后置 checkpoint 生成和 manifest artifact writeback callback 接线；只调用注入 callback，不判断 dirty、不构造 blocked report、不推进状态。 |
| `coding_orchestration/run_manifest_session_writeback_service.py` | Run manifest session metadata writeback host service：承接 runner session metadata 到 run manifest 的 host callback 接线；只消费已解析 session id，复用 manifest session 字段投影并调用注入 writer，不解析 stdout、不写 ledger/report/summary、不启动 runner 或推进状态。 |
| `coding_orchestration/run_completion_writeback_service.py` | Completed run writeback coordinator host service：承接 fresh completed run 的 completion projection、stale observation、状态 transition、ledger/session/summary/project writeback 和 final result payload；只通过注入 callback 工作，不启动 runner、不执行 diff guard、不收集 QA evidence、不判断 dirty 或准备 checkpoint。 |
| `coding_orchestration/run_reconcile_writeback_service.py` | Active run reconcile writeback coordinator host service：承接 active run reconcile 完成态的 completion projection、最终 report 写回、状态 transition、ledger upsert、runner session update、summary writer callback 和 result payload；只消费已归一化 report 与注入 callback，不读取 workspace、不启动 runner、不执行 diff guard、不收集 QA evidence、不判断 dirty 或准备 checkpoint。 |
| `coding_orchestration/run_start_selection_projection.py` | Run start selection projection helper：run context source、QA/merge-test checkpoint 选择、checkpoint failed 判定、QA evidence observation、source branch recording、project path requirement、workspace selection 和 manifest checkpoint preparation selection 纯规则；不读取 workspace、不准备 checkpoint、不写 manifest、不推进 ledger。 |
| `coding_orchestration/run_session_projection.py` | Run session projection helper：plan report session fields 白名单、plan report session update、run start base/workspace session update、active run session update、runner session update 和 completion session update 纯 payload；不写 ledger、不生成 attach command、不读取 artifact、不推进状态。 |
| `coding_orchestration/run_session_writeback_service.py` | Run session writeback host service：处理 start_run 与 active run reconcile 的 task session callback；只消费 session projection 已构造的 update dict 并调用注入 callback，空 update 跳过，不直接 import `TaskLedger` / storage repository、不构造 payload、不写 artifact、不启动 runner 或推进状态。 |
| `coding_orchestration/run_prompt_projection.py` | Run prompt projection helper：首次 prompt 与增量 prompt 构造选择和参数合同；只调用传入的 `PromptBuilder` 生成字符串，不写 `input-prompt.md`、不生成 context artifact、不写 manifest、不启动 runner、不写 ledger。 |
| `coding_orchestration/run_context_artifact_service.py` | Run context artifact service：写入 wiki context、confirmed plan / implementation context、assembled context、run instructions、execution policy 和 context index，并读取 `execution-policy.json`；只触达 run_dir 下的 context artifact，不写 ledger、manifest、report、summary，不启动 runner 或推进状态。 |
| `coding_orchestration/run_start_artifact_service.py` | Run start artifact service：写入 `report.schema.json`、`input-prompt.md` 和 `run-manifest.json`；只写 run_dir 下启动 artifact，不准备 checkpoint、不写 ledger/report/summary、不启动 runner 或推进状态。 |
| `coding_orchestration/run_manifest_artifact_service.py` | Run manifest artifact service：写入指定 `run-manifest.json`；只负责 manifest artifact 写回，不写 report/summary/ledger，不启动 runner 或推进状态。 |
| `coding_orchestration/run_stderr_artifact_service.py` | Run stderr artifact service：写入指定 `stderr.log`；只负责 stderr artifact 落盘，不写 report/summary/manifest/ledger，不启动 runner 或推进状态。 |
| `coding_orchestration/run_report_artifact_service.py` | Run report artifact service：读写指定 `report.json`，并读取 `summary_markdown` 摘要 excerpt；只负责 report artifact 文件边界，缺失或无效读取返回空 dict / 空摘要，不生成 report，不写 manifest/summary/ledger，不启动 runner 或推进状态。 |
| `coding_orchestration/run_summary_artifact_service.py` | Run summary artifact service：读写指定 `summary.md`；只负责 summary artifact 文件边界，不生成 summary，不写 report/manifest/ledger，不启动 runner 或推进状态。 |
| `coding_orchestration/run_ledger_projection.py` | Run ledger writeback projection：构造 start_run 与 active run reconcile 的 artifact / agent_run / merge-test record 写回 payload；只返回纯数据，不调用 ledger、不写 artifact、不启动 runner 或推进状态。 |
| `coding_orchestration/run_ledger_writeback_service.py` | Run ledger writeback host service：处理 completed run 与 active run reconcile 的 ledger callback；只消费 ledger projection 生成的 records 并调用注入 callback，不构造 payload、不直接 import `TaskLedger` / storage repository、不写 artifact、不启动 runner或推进状态。 |
| `coding_orchestration/run_summary_projection.py` | Run summary writeback projection：构造 completed run 与 active run reconcile 写入 LLM Wiki run summary 的 writer payload；只返回纯数据，不读取 summary artifact、不调用 summary writer、不写 LLM Wiki、不写 ledger 或推进状态。 |
| `coding_orchestration/run_summary_writeback_service.py` | Run summary writeback host service：处理 completed run 与 active run reconcile 的 summary writer callback；复用 summary projection 构造 payload，只调用注入 writer，不读写 artifact/ledger，不启动 runner 或推进状态。 |
| `coding_orchestration/run_artifact_paths.py` | Run artifact path projection：构造 fresh run_dir 和 existing run record 的 `ArtifactSet` 路径合同；只返回路径，不读写 artifact 文件、不写 ledger、不启动 runner 或推进状态。 |
| `coding_orchestration/run_project_writeback_service.py` | Run project writeback host service：处理 completed run 的 Project/WorkItem writeback stale gate、payload 构造委托和注入 callback 调用；不直接 import WorkItemService/MCP adapter，不写 ledger/artifact，不启动 runner 或推进状态。 |
| `coding_orchestration/run_orchestration_service.py` | Run orchestration 迁移期 application helper：observed run report 构造、stale completion 观测、execution policy decision 读取、run-level diff guard violations 组合、verification limitations fallback projection、completion report payload 构造、agent run record 构造、reconciled agent run record 构造、reconcile result payload 构造、existing run mode/changed files 规则、merge-test run record 构造、project writeback payload 构造、start_run final result payload 构造和 run completion 状态/phase/report 投影；兼容 re-export run failure report、run report refinement、run start selection、run session 和 run prompt projection helper；不承载 runner subprocess、Gateway 发送、workspace/git 副作用或后台 host orchestration。 |
| `coding_orchestration/gateway_binding_service.py` | Gateway 会话绑定 host helper：event source、chat/user binding key、active task、coding mode、active project、pending rewrite/action 和 stale binding 清理；不承载确认后命令执行、业务 gate 或状态推进。 |
| `coding_orchestration/workspace_checkpoint_service.py` | Workspace/Git checkpoint run support helper：implementation workspace 复用/创建、source branch/base branch、QA artifact 收集、clean-tree checkpoint、git HEAD 和 diff guard QA artifact 过滤；不承载 runner 启动、任务状态推进或用户可见风险决策。 |
| `coding_orchestration/run_manifest_service.py` | Run manifest/session policy helper：run-manifest 基础字段、启动期 manifest update projection、artifact record、Codex attach/resume 展示命令、manifest session metadata 字段投影、controlled bypass 权限 profile、source elevated plan 权限判断；不承载 runner 启动、状态推进、checkpoint 准备、host 写回 callback 或 subprocess 命令执行。 |
| `coding_orchestration/ledger.py` | SQLite Task Ledger 兼容 façade，保留既有公开方法并委托 storage repositories。 |
| `coding_orchestration/storage/schema.py`、`storage/repositories.py`、`storage/task_repository.py`、`storage/run_repository.py`、`storage/artifact_repository.py`、`storage/binding_repository.py`、`storage/common.py` | Storage-owned SQLite schema、兼容 migration、task/run/artifact/binding repository；`storage/repositories.py` 只保留兼容 re-export，SQL mutation 按 repository 文件归属，`TaskLedger` 不再内联建表、字段补齐或 SQL mutation。 |
| `coding_orchestration/project_resolver.py`、`project_knowledge_resolver.py`、`project_knowledge_initializer.py`、`project_knowledge_inventory.py`、`project_knowledge_documents.py`、`knowledge_adapter.py` | 项目识别、KnowledgePort project profile 消费、本地 LLM Wiki adapter 和项目知识初始化；initializer 保留兼容 façade，inventory scanner 负责仓库扫描/分类/技术栈推断，document builder 负责 LLM Wiki 文档生成。 |
| `coding_orchestration/source_links.py`、`source_recovery.py`、`source_work_item_context.py`、`source_projection.py`、`source_resolver.py`、`feishu_project_reader.py`、`feishu_document_reader.py`、`feishu_work_item_reader.py`、`meegle_reader.py` | 飞书 Wiki/Docx、飞书 Project/Meegle 来源解析与权限预检；URL/identity parsing 已归属纯 `source_links.py`，deferred recovery payload 和 CLI command shape 已归属纯 `source_recovery.py`，Feishu/Meegle work item payload 归一化已归属纯 `source_work_item_context.py`，`source_projection.py` 将 `SourceResult` / legacy `source_context` 投影为 prompt/context/status/manifest/run-preflight/dashboard source health/source-resolve diagnostic payload/task creation helper 可消费的稳定字段，Feishu Docx/Wiki gateway / `lark-cli` document reader 已归属 `feishu_document_reader.py`，Feishu Project work item gateway / OpenAPI env reader 已归属 `feishu_work_item_reader.py`，`SourceResolver.resolve_source_result()` 统一输出 `SourceResult`，旧 `resolve_source()` 仅保留兼容 dict context。 |
| `coding_orchestration/services/task_service.py`、`services/task_utils.py` | Hermes task 应用服务：`TaskService` 和 task_utils 维护结构化任务创建、文本建 task、来源索引/规范化、状态 payload、task creation helper 和 task 级纯规则。 |
| `coding_orchestration/services/run_service.py` | 运行生命周期应用服务：运行启动 blocker、实现前置条件、mode 标签、timeout/running phase 和 runner 结果到 task 状态/phase 的映射。 |
| `coding_orchestration/services/delivery_service.py`、`delivery_command_executor.py` | 交付拆解应用服务与 command host shell：`DeliveryService` 维护 decomposition session 投影、breakdown approval、materialize execution task 编排、delivery status projection、run-next decision、next runnable child、requirement rollup 状态和 phase 规则；`delivery_command_executor.py` 维护 `/coding breakdown`、`/coding analyze`、`/coding approve-breakdown`、`/coding materialize`、`/coding run <parent> --next`、`/coding status <parent> --delivery/--tree` 用户可见 host shell 和 decomposition / human decision / ledger / rollup callback 绑定。 |
| `coding_orchestration/coding_diagnostics_command_executor.py` | Coding diagnostics command host shell：维护 `doctor`、`lark-preflight`、`project-mcp-preflight`、`source-resolve` 的 command-mode dispatch、Project MCP config/readiness gate、Hermes runtime availability probe、Gateway immediate diagnostic 分支和 `doctor_presenter.py` direct 文案调用；不承载 Gateway route 规则、CLI direct dispatcher、普通 status/list presentation、runner/workspace/git 或 run lifecycle。 |
| `coding_orchestration/services/workitem_service.py`、`services/workitem_utils.py` | 飞书 Project 工作项应用服务：MCP preflight、Story/Issue intake、WBS 更新、状态流转和 bugfix 回写。 |
| `coding_orchestration/prompt_builder.py`、`coding_orchestration/prompts/`、`context_assembler.py` | Codex 可见 prompt 与运行上下文 artifact 组装；mode/source 模板归属在 `prompts/`，`PromptBuilder` 只做组合。 |
| `coding_orchestration/runner_router.py`、`coding_orchestration/runners/` | runner 选择与 Codex CLI / Hermes autonomous Codex / generic CLI 适配；Codex command 构造已拆到 `runners/codex_command.py`，subprocess/timeout/cancel/timing 已拆到 `runners/codex_process.py`，report 字段/状态/stdout 解析策略已拆到 `runners/codex_report.py`，report schema 已拆到 `runners/codex_report_schema.py`，report 读取/admission/fallback 决策已拆到 `runners/codex_report_loader.py`，report/summary 写入已拆到 `runners/codex_report_writer.py`，artifact 路径合同已拆到 `runners/codex_artifacts.py`。 |
| `coding_orchestration/report_contract.py`、`report_admission.py`、`run_summary_writer.py`、`run_log_compactor.py` | runner report 质量门、交付拆解准入、run summary 端口委托和日志压缩。 |
| `coding_orchestration/diff_guard.py` | implementation/QA/merge-test 后的路径边界审计。 |
| `coding_orchestration/dashboard/` | Hermes dashboard tab API 和 manifest。 |
| `coding_orchestration/skills/coding-operator-core/`、`skills/coding-health-core/` | host-agnostic skill 合同：通用意图分流、项目优先、交付拆解、readiness 输出格式和修复口径。 |
| `coding_orchestration/skills/hermes-coding-operator/`、`skills/hermes-coding-health-check/` | Hermes host binding skill：把 core intent / health check 映射到 `/coding`、Hermes CLI、`lark-cli` 和插件本地配置。 |
| `scripts/` | 本地软链接安装、卸载和架构治理检查入口；`architecture_guard.py` 负责行数、hard code、边界漂移和真实 token 模式扫描；`scripts/release_readiness.py` 负责发布 readiness gate，串联完整单测、architecture guard、`git diff --check`、敏感扫描和最小 Hermes smoke。 |
| `tests/test_architecture_guard.py` | 测试治理守门：确认 resolved legacy flow suite 不再保留 large-file 豁免；旧私有 helper 断言改写为 service/policy/presenter/adapter contract tests，旧文件形态覆盖从 `tests/test_orchestrator_run_flow.py` 迁到行为域小文件后，只保留 smoke/transition/sandbox。 |
| `tests/` | `unittest` 测试，覆盖插件注册、状态机、orchestrator、runner、source、dashboard、安装脚本等；`orchestrator_flow_fixtures.py` 承载共享测试 fixture，`test_orchestrator_run_flow.py` 只保留端到端 smoke、kanban transition 和 plan-only resume sandbox；`test_delivery_flow.py` 承载 delivery/materialize/run-next flow，`test_source_flow.py` 和 `test_source_plan_flow.py` 承载来源索引、deferred source 与 source plan-run flow，`test_run_background_orchestration.py` 承载后台 run 等待完成、后台失败收敛和 merge-test pending action host orchestration contract，`test_run_failure_report_projection.py` 承载 runner/checkpoint failure report projection 独立模块 contract，`test_run_report_refinement_projection.py` 承载 diff guard / implementation commit missing blocked report 与 run report refinement projection 独立模块 contract，`test_run_diff_guard_service.py` 承载 run diff guard observation host service contract，`test_run_dispatch_service.py` 承载 run dispatch host service contract，`test_run_status_transition_service.py` 承载 run status transition host service contract，`test_run_evidence_observation_service.py` 承载 run evidence observation host service contract，`test_run_checkpoint_preparation_service.py` 承载 run checkpoint preparation host service contract，`test_run_start_selection_projection.py` 承载 run context source、checkpoint selection、QA evidence、source branch recording、project path requirement、workspace selection 和 manifest checkpoint preparation 独立模块 contract，`test_run_session_projection.py` 承载 run session projection 独立模块 contract，`test_run_session_writeback_service.py` 承载 run session host callback contract，`test_run_prompt_projection.py` 承载 run prompt projection 独立模块 contract，`test_run_summary_projection.py` 承载 completed run 与 active run reconcile 的 summary writer payload projection contract，`test_run_summary_writeback_service.py` 承载 completed run 与 active run reconcile 的 summary writer host callback contract，`test_run_ledger_writeback_service.py` 承载 completed run 与 active run reconcile 的 run ledger host callback contract，`test_run_artifact_paths.py` 承载 fresh/existing run 的 `ArtifactSet` path projection contract，`test_run_project_writeback_service.py` 承载 run completion project writeback host gate/callback contract，`test_run_orchestration_plan_report_session.py` 保留 plan report / completion session 兼容 re-export contract，`test_run_orchestration_service.py` 承载 completion report payload、agent run record、reconciled agent run record、reconcile result payload、merge-test run record、project writeback payload、start_run result payload 和 run completion projection helper contract，`test_run_orchestration_reconcile_rules.py` 承载 existing run mode/changed files 规则 contract，`test_run_orchestration_start_rules.py` 承载 observed run report、stale completion 观测、blocked/refinement projection wrapper、execution policy decision 读取、run start/active session update、run-level diff guard violations 组合规则和 verification limitations fallback projection contract，`test_run_orchestration_workspace_rules.py` 保留 workspace selection 兼容 re-export contract，`test_plan_run_flow.py` 承载 plan-only run/completion/只读 gate flow，`test_implementation_result_flow.py`、`test_implementation_workspace_flow.py` 和 `test_implementation_session_flow.py` 承载 implementation 结果、workspace/branch 和 session/prompt flow，`test_command_run_flow.py` 承载 `/coding run` / `/coding implement` 命令 flow，`test_coding_run_command_executor.py` 承载普通 run / implement / QA command-mode host shell contract，`test_coding_feedback_command_executor.py` 承载普通 continue/change/bugfix command-mode 与 Gateway active feedback host shell contract，`test_coding_task_list_command_executor.py` 承载普通 list 与 Gateway list host shell contract，`test_coding_diagnostics_command_executor.py` 承载 doctor/preflight/source-resolve diagnostic host shell contract，`test_coding_status_command_executor.py` 承载普通 status 与 Gateway status host shell contract，`test_gateway_command_controller.py` 承载 `/coding` 命令归一化、确认/取消分类、rewrite 风险确认、Gateway event dedupe 和授权探测 contract，`test_gateway_command_executor.py` 承载 route metadata 到 custom command handler 的 host shell dispatch contract，`test_gateway_pending_action_executor.py` 承载 pending action 确认/取消和 latest human_required fallback host shell contract，`test_gateway_active_context.py` 承载 active project 应用到 task 的 host helper contract，`test_gateway_command_group_flow.py`、`test_gateway_project_task_flow.py`、`test_gateway_task_control_flow.py`、`test_gateway_feedback_flow.py`、`test_gateway_change_continue_flow.py` 和 `test_gateway_safety_lifecycle_flow.py` 承载 gateway 命令组、项目/任务路由、确认/use/delete/continue、bugfix/change/continue feedback 和安全生命周期 flow；`test_feedback_presenter.py` 承载反馈、需求变更、图片未捕获和人工澄清用户可见文案 contract；`test_merge_test_presenter.py` 承载 prepare/merge-test 状态提示、blocked/QA 风险确认和放行说明文案 contract；`test_status_reconcile_flow.py` 承载状态展示、active run reconcile 和 execution policy flow；`test_qa_flow.py` 承载手动 QA、QA artifact 和 QA clean-tree gate flow，`test_completion_flow.py` 承载 merged-test 后人工 complete/list flow，`test_cancel_restore_flow.py` 承载 cancel/restore flow，`test_merge_test_basic_flow.py`、`test_merge_test_readiness_flow.py`、`test_merge_test_blocked_flow.py` 和 `test_merge_test_qa_gate_flow.py` 承载 merge-test 手动门禁、blocked readiness、风险确认和 QA gate flow，`test_bugfix_writeback_flow.py` 承载 bugfix 完成后的 Project writeback flow；`test_codex_cli_command_facade.py`、`test_codex_cli_process_facade.py`、`test_codex_cli_report_facade.py` 和 `test_codex_cli_report_failure_facade.py` 承载 CodexCliRunner 兼容 façade flow，底层 command/process/report/writer/artifact 合同由相邻 `test_codex_*` 小文件维护，run artifact 独立 writer 合同由 `test_run_*_artifact_service.py` 维护。 |
| `tests/test_run_context_artifact_service.py` | Run context artifact service contract：覆盖 wiki context、confirmed plan / implementation context、assembled context、run instructions、execution policy、context index、QA fallback 写入边界和 `execution-policy.json` 读取边界。 |
| `tests/test_run_start_artifact_service.py` | Run start artifact service contract：覆盖 `report.schema.json`、`input-prompt.md` 和 `run-manifest.json` 启动 artifact 写入边界，确保不写 `report.json` 或 `summary.md`。 |
| `tests/test_run_manifest_artifact_service.py` | Run manifest artifact service contract：覆盖 `run-manifest.json` 写回边界、返回路径和不写 report/summary 的约束。 |
| `tests/test_run_stderr_artifact_service.py` | Run stderr artifact service contract：覆盖 `stderr.log` 写回边界、返回路径和不写 report/summary/manifest 的约束。 |
| `tests/test_run_report_artifact_service.py` | Run report artifact service contract：覆盖 `report.json` 读取、summary excerpt、写回、返回路径和不写 manifest/summary 的约束。 |
| `tests/test_run_summary_artifact_service.py` | Run summary artifact service contract：覆盖 `summary.md` 读取、写回、返回路径和不写 report/manifest 的约束。 |
| `tests/test_run_diff_guard_service.py` | Run diff guard observation host service contract：覆盖 before snapshot、changed files 观测、QA report artifact 过滤、policy violations、diff summary 写回和 `start_run()` 委托边界。 |
| `tests/test_run_reconcile_writeback_service.py` | Active run reconcile writeback coordinator contract：覆盖 active run reconcile 完成态 report finalization、transition、ledger upsert、runner session update、summary writer callback、result payload 和 orchestrator 委托边界。 |
| `tests/test_run_dispatch_service.py` | Run dispatch host service contract：覆盖 checkpoint failure 不启动 runner、runner 成功调度、runner exception fallback 和 `start_run()` 委托边界。 |
| `tests/test_run_evidence_observation_service.py` | Run evidence observation host service contract：覆盖 QA evidence disabled/enabled、tested commit、implementation dirty-check callback gate 和 `start_run()` 委托边界。 |
| `tests/test_run_checkpoint_preparation_service.py` | Run checkpoint preparation host service contract：覆盖 none/QA/merge-test checkpoint preparation、空 payload 不写 manifest update 和 `start_run()` 委托边界。 |
| `tests/test_run_implementation_checkpoint_service.py` | Run implementation checkpoint writeback host service contract：覆盖 dirty=false 跳过、dirty=true 生成 implementation checkpoint 并写回 manifest、dict/object manifest update 和 `start_run()` 委托边界。 |
| `tests/test_run_manifest_session_writeback_service.py` | Run manifest session metadata writeback host service contract：覆盖 session_id 缺失跳过、session_id 存在时 manifest 字段投影和 writer callback，以及 `start_run()` 委托边界。 |
| `tests/test_run_completion_writeback_service.py` | Completed run writeback coordinator host service contract：覆盖 fresh completed run 写回协调、stale completion 语义和 `start_run()` 委托边界。 |
| `tests/test_run_ledger_projection.py` | Run ledger writeback projection contract：覆盖 start_run 与 active run reconcile 的 artifact / agent_run / merge-test record 写回 payload 聚合边界，确保模块只返回 payload、不调用 ledger。 |
| `tests/test_run_ledger_writeback_service.py` | Run ledger writeback host service contract：覆盖 completed run append、active run reconcile upsert、merge-test record skip/append 和 orchestrator 委托边界。 |
| `tests/test_run_session_writeback_service.py` | Run session writeback host service contract：覆盖非空 update 调用注入 callback、空 update 跳过、start_run、start_run 异常清理与 active run reconcile 的 orchestrator 委托边界。 |
| `tests/test_run_summary_projection.py` | Run summary writeback projection contract：覆盖 completed run 与 active run reconcile 写入 LLM Wiki run summary 的 writer payload 聚合边界，确保模块只返回 payload、不读取 summary artifact、不调用 summary writer。 |
| `tests/test_run_project_writeback_service.py` | Run project writeback host service contract：覆盖 stale completion 跳过 Project writeback、fresh completion callback payload 和 `start_run()` 委托边界。 |
| `tests/test_run_manifest_service.py` | Run manifest service contract：覆盖 run-manifest 基础字段、启动期 manifest update projection、Codex attach/resume 展示命令、controlled bypass 权限 profile、source elevated plan 权限判断、manifest session metadata 字段投影和 artifact record。 |
| `examples/` | 项目 registry 与 `WORKFLOW.md` 示例。 |
| `docs/` | 项目沉淀文档、bootstrap 合同文档、部署/前置准备、状态机、交付流和实施计划。 |
| `docs/plans/` | superpowers 或执行计划产出的实施计划归档；该目录作为项目沉淀提交。 |
| `docs/local/presentations/` | 本地宣讲、demo、分享材料目录；该目录被 `.gitignore` 忽略，不作为项目事实来源。 |
| `README.md`、`PLUGIN_USAGE.md`、`PLUGIN_PREREQUISITES.md` | 使用说明、前置准备和运行边界。 |

## 运行链路

```text
Feishu / Hermes Gateway
  -> coding_orchestration pre_gateway_dispatch
  -> CodingOrchestrator
  -> TaskService / RunService / WorkItemService + TaskLedger + ProjectResolver + SourceResolver + LLM Wiki
  -> PromptBuilder + ContextAssembler
  -> RunnerRouter
  -> CodexCliRunner / HermesAutonomousCodexRunner / GenericCliRunner
  -> report.json / summary.md / diff.patch / run-log.md
  -> TaskLedger + 飞书状态回写 + LLM Wiki run_summary
```

## Guarded Path

| 路径 | 原因 |
| --- | --- |
| `.env*` | 可能包含 Hermes、飞书、Codex 或项目密钥。 |
| `~/.hermes/.env`、`~/.hermes/auth.json`、`~/.codex/auth.json` | 本地凭据，只能读取必要状态，不得复制进仓库。 |
| `~/.hermes/coding-orchestration/ledger.db`、`runs/`、`workspaces/`、`llm-wiki/` | 运行期事实和产物，不是源码事实层。 |
| `coding_orchestration/orchestrator.py` | 状态流、人工门禁和 runner 调度仍集中在这里，改动需要配套状态/流程测试；source context 读取、deferred source pre-run enrichment 和既有 task context 修复优先走 `source_context_repair_service.py`，后台 run mode-specific 启动和通知接线优先走 `coding_background_run_executor.py`，后台通知细节优先走 `background_run_notifier.py`，后台等待完成、失败状态收敛和 merge-test pending action 优先走 `run_background_orchestration.py`，runner/checkpoint failure report projection 优先走 `run_failure_report_projection.py`，diff guard / implementation commit missing blocked report 与 run report refinement projection 优先走 `run_report_refinement_projection.py`，run diff guard observation 优先走 `run_diff_guard_service.py`，QA artifact/tested commit observation 和 implementation dirty-check observation 优先走 `run_evidence_observation_service.py`，run context source、run checkpoint 选择、QA evidence observation、source branch recording、project path requirement、workspace selection 和 manifest checkpoint preparation selection 纯规则优先走 `run_start_selection_projection.py`，plan report session fields 白名单、plan report session update、run start base/workspace session update、active run session update、completion session update 和 runner session update 优先走 `run_session_projection.py`，对应 task session callback 优先走 `run_session_writeback_service.py`，首次/增量 prompt 构造选择优先走 `run_prompt_projection.py`，run context artifact 写入和 execution policy artifact 读取优先走 `run_context_artifact_service.py`，run start artifact 写入优先走 `run_start_artifact_service.py`，run manifest artifact 写回优先走 `run_manifest_artifact_service.py`，run manifest session metadata host 写回优先走 `run_manifest_session_writeback_service.py`，run stderr artifact 写回优先走 `run_stderr_artifact_service.py`，run report artifact 读写优先走 `run_report_artifact_service.py`，run summary artifact 读写优先走 `run_summary_artifact_service.py`，start_run 与 active run reconcile 的 run ledger writeback payload 聚合优先走 `run_ledger_projection.py`，run ledger host callback 优先走 `run_ledger_writeback_service.py`，completed run 与 active run reconcile 的 run summary writer payload 聚合优先走 `run_summary_projection.py`，fresh/existing run 的 `ArtifactSet` 路径合同优先走 `run_artifact_paths.py`，run project writeback host gate/callback 优先走 `run_project_writeback_service.py`，observed run report、stale completion 观测、execution policy decision 读取、run-level diff guard violations 组合、verification limitations fallback projection、completion report payload、agent run record 构造、reconciled agent run record 构造、reconcile result payload、existing run mode/changed files 规则、merge-test run record、project writeback payload、start_run result payload 和 run completion 投影优先走 `run_orchestration_service.py`，Gateway 会话绑定存取优先走 `gateway_binding_service.py`，`/coding` / `/commands` 解析、命令归一化、命令 route plan、handler key、reply mode、task id 来源策略、merge-test 参数、rewrite canonical command、确认/取消词、rewrite 风险确认、event dedupe 和授权探测优先走 `gateway_command_controller.py`，Gateway custom route 分发优先走 `gateway_command_executor.py`，Gateway coding mode host shell 优先走 `gateway_coding_mode_executor.py`，pending action 确认/取消路由优先走 `gateway_pending_action_executor.py`，Gateway route façade 优先走 `orchestrator_gateway_facade.py`，project/profile façade 优先走 `orchestrator_project_facade.py`，task/source façade 优先走 `orchestrator_task_source_facade.py`，普通 list host shell 优先走 `coding_task_list_command_executor.py`，普通 status host shell 优先走 `coding_status_command_executor.py`，普通 run/implement/QA command host shell 优先走 `coding_run_command_executor.py`，普通 continue/change/bugfix feedback host shell 优先走 `coding_feedback_command_executor.py`，普通 use/exit/cancel/restore/delete/complete task control host shell 优先走 `coding_task_control_command_executor.py`，project list/init/use/status/clear host shell 优先走 `project_command_executor.py`，tool operation façade 优先走 `orchestrator_tool_facade.py`，diagnostics/dashboard façade 优先走 `orchestrator_diagnostics_facade.py`，active project 应用到 task 优先走 `gateway_active_context.py`，workspace/git/checkpoint helper 优先走 `workspace_checkpoint_service.py`，run manifest 启动期字段投影、session metadata 字段投影和 policy 优先走 `run_manifest_service.py`。 |
| `coding_orchestration/merge_test_readiness_service.py` | blocked merge-test readiness 评估边界；只允许基于上游传入的 task/run/workspace/session/report 事实返回评估字段，不得读取 workspace/report 文件、写 ledger、append merge record、推进状态、触发 runner 或发送 Gateway 回复。 |
| `coding_orchestration/project_profile_catalog.py` | project profile catalog 边界；只允许读取 wiki project profile、registry project facts 和初始化质量评估，返回 profile/list/status 数据或文案，不得写 LLM Wiki、绑定 active project、创建任务、推进状态或发送 Gateway 回复。 |
| `coding_orchestration/project_command_executor.py` | project context command host shell 边界；只允许维护 `/coding project list/init/use/status/clear` 用户可见分支、profile upsert callback 和 active project binding callback，不得实现 Gateway route、项目画像 catalog 纯规则、任务创建、runner 启动、workspace/git 或状态推进。 |
| `coding_orchestration/run_context_artifact_service.py` | run_dir context artifact 读写边界；只允许写 wiki context、confirmed plan / implementation context、assembled context、run instructions、execution policy 和 context index，并读取 `execution-policy.json`；不得写 ledger、manifest、report、summary，不得启动 runner 或推进状态。 |
| `coding_orchestration/run_start_artifact_service.py` | run_dir 启动 artifact 写入边界；只允许写 `report.schema.json`、`input-prompt.md` 和 `run-manifest.json`，不得准备 checkpoint、写 ledger/report/summary、启动 runner 或推进状态。 |
| `coding_orchestration/run_manifest_artifact_service.py` | run_dir manifest artifact 写回边界；只允许写指定 `run-manifest.json`，不得写 report/summary/ledger，不得启动 runner 或推进状态。 |
| `coding_orchestration/run_stderr_artifact_service.py` | run_dir stderr artifact 写回边界；只允许写指定 `stderr.log`，不得写 report/summary/manifest/ledger，不得启动 runner 或推进状态。 |
| `coding_orchestration/run_report_artifact_service.py` | run_dir report artifact 读写边界；只允许读写指定 `report.json` 和 summary excerpt，不得生成 report 内容、写 manifest/summary/ledger，不得启动 runner 或推进状态。 |
| `coding_orchestration/run_summary_artifact_service.py` | run_dir summary artifact 读写边界；只允许读写指定 `summary.md`，不得生成 summary 内容、写 report/manifest/ledger，不得启动 runner 或推进状态。 |
| `coding_orchestration/run_diff_guard_service.py` | run diff guard observation 边界；只允许执行 before snapshot、changed files 观测、QA report artifact 过滤、policy violations 组合和指定 diff summary 写回，不得写 ledger/report/manifest/summary、启动 runner、收集 QA evidence 或推进状态。 |
| `coding_orchestration/run_dispatch_service.py` | run dispatch host 边界；只允许处理 checkpoint failure gate、调用注入 runner 和 runner exception fallback callback，不得写 ledger/report/manifest/summary、执行 diff guard、收集 QA evidence 或推进状态。 |
| `coding_orchestration/run_status_transition_service.py` | run status transition host 边界；只允许处理状态机、status/phase/Kanban callback、run start/completion/reconcile transition 和 active run cleanup，不得写 report/manifest/summary、append/upsert ledger run records、启动 runner、执行 diff guard 或收集 QA evidence。 |
| `coding_orchestration/run_evidence_observation_service.py` | run evidence observation 边界；只允许通过注入 callback 观测 QA artifacts、tested commit 和 implementation dirty-check，不得写 ledger/report/manifest/summary、创建 checkpoint、启动 runner、执行 diff guard 或推进状态。 |
| `coding_orchestration/run_checkpoint_preparation_service.py` | run checkpoint preparation 边界；只允许通过注入 callback 准备 QA / merge-test checkpoint，并返回 manifest update dict；不得直接 mutate manifest、写 artifact/ledger/report/summary、启动 runner、执行 diff guard 或推进状态。 |
| `coding_orchestration/run_implementation_checkpoint_service.py` | run implementation checkpoint writeback 边界；只允许在 dirty flag 已由上游计算后，通过注入 callback 生成 implementation checkpoint 并写回 manifest artifact；不得判断 dirty、不构造 blocked report、不写 ledger/report/summary、不启动 runner或推进状态。 |
| `coding_orchestration/run_manifest_session_writeback_service.py` | run manifest session metadata 写回边界；只允许消费已解析的 session id，更新 manifest object/dict 并调用注入的 manifest metadata writer；不得解析 stdout、不写 ledger/report/summary、不启动 runner 或推进状态。 |
| `coding_orchestration/run_completion_writeback_service.py` | fresh completed run 写回协调边界；只允许消费已完成 runner result 的投影数据和注入 callback，协调 report artifact、状态 transition、ledger/session/summary/project writeback 与 result payload；不得启动 runner、执行 diff guard、收集 QA evidence、判断 dirty、准备 checkpoint、解析 stdout 或直接持有 host adapter。 |
| `coding_orchestration/run_ledger_projection.py` | run ledger 写回 payload 边界；只允许构造 start_run 与 active run reconcile 的 artifact、agent_run 和 merge-test record payload，不得调用 ledger、不写 artifact、不启动 runner 或推进状态。 |
| `coding_orchestration/run_ledger_writeback_service.py` | run ledger 写回 host callback 边界；只允许消费 `RunLedgerWritebackRecords` / `ReconciledRunLedgerWritebackRecords` 并调用注入的 ledger callback，不得构造 payload、直接 import `TaskLedger` / storage repository、写 artifact、启动 runner 或推进状态。 |
| `coding_orchestration/run_session_writeback_service.py` | run session 写回 host callback 边界；只允许消费 session projection 已构造的 update dict 并调用注入的 task session callback，空 update 跳过，不得构造 payload、直接 import `TaskLedger` / storage repository、写 artifact、启动 runner 或推进状态。 |
| `coding_orchestration/run_summary_projection.py` | run summary writer payload 边界；只允许构造 completed run 与 active run reconcile 的 LLM Wiki run summary 写回参数，不得读取 summary artifact、调用 summary writer、写 LLM Wiki、写 ledger 或推进状态。 |
| `coding_orchestration/run_artifact_paths.py` | run artifact path projection 边界；只允许构造 fresh run_dir 和 existing run record 的 `ArtifactSet` 路径合同，不得读写 artifact 文件、创建目录、调用 ledger、启动 runner 或推进状态。 |
| `coding_orchestration/run_project_writeback_service.py` | run project writeback host 边界；只允许处理 stale completion gate、调用 project writeback payload projection 和注入的 writeback callback，不得直接 import WorkItemService/MCP adapter、写 ledger/artifact、启动 runner 或推进状态。 |
| `coding_orchestration/config.py`、`tool_specs.py`、`ports.py` | 新解耦合同层，改动会影响后续服务/adapter 拆分方向，需保持 host-agnostic。 |
| `coding_orchestration/services/task_service.py`、`services/task_utils.py` | 任务创建、来源索引和状态 payload 用例层；改动需配套 `tests/test_task_service.py` 和 orchestrator 主流程回归。 |
| `coding_orchestration/services/run_service.py` | 运行策略和结果映射用例层；改动需配套 `tests/test_run_service.py`、`tests/test_plan_run_flow.py`、`tests/test_command_run_flow.py`、`tests/test_status_reconcile_flow.py` 和 runner 回归。 |
| `coding_orchestration/services/delivery_service.py`、`delivery_command_executor.py` | 父级需求交付拆解纯规则与 command host shell；改动需配套 `tests/test_delivery_service.py`、`tests/test_delivery_command_executor.py` 和 requirement delivery 主流程回归。 |
| `coding_orchestration/project_command_executor.py` | 项目上下文命令 host shell；改动需配套 `tests/test_project_command_executor.py` 和 Gateway project task / rewrite flow 回归。 |
| `coding_orchestration/status_policy.py` | 状态详情和 report 投影策略；改动需配套 `tests/test_status_policy.py`、orchestrator 主流程和 report 回归。 |
| `coding_orchestration/services/workitem_service.py` | 飞书 Project 工作项用例层；改动需配套 `tests/test_workitem_service.py` 和 orchestrator tool 回归。 |
| `coding_orchestration/storage/schema.py`、`storage/*_repository.py`、`storage/common.py`、`storage/repositories.py` | ledger schema / migration / repository 合同；`repositories.py` 是兼容 re-export，具体 SQL mutation 改动需落在对应 repository 文件，并配套 `tests/test_storage_schema.py`、`tests/test_storage_repositories.py` 和 ledger 行为回归。 |
| `coding_orchestration/runners/codex_cli.py`、`runners/codex_command.py`、`runners/codex_process.py`、`runners/codex_report.py`、`runners/codex_report_schema.py`、`runners/codex_report_loader.py`、`runners/codex_report_writer.py`、`runners/codex_artifacts.py` | Codex CLI 权限 flag、sandbox、resume、subprocess/cancel、report 字段策略、output schema、report 读取决策、report/summary 写入和 artifact 路径，改动可能影响安全边界。 |
| `coding_orchestration/skills/coding-operator-core/`、`skills/coding-health-core/` | Skill core contract，必须保持 host-agnostic，不写 Hermes 命令、运行根、内部账本路径或本机凭据。 |
| `coding_orchestration/skills/hermes-coding-operator/`、`skills/hermes-coding-health-check/` | Hermes binding skill，只负责 host 映射和用户可见修复命令；改动需配套 `tests/test_plugin_registration.py`。 |
| `coding_orchestration/install.py`、`scripts/install_symlink.py`、`scripts/uninstall_legacy.py` | 会影响本机 Hermes 插件安装、卸载和 Gateway 重启，执行前需先 dry-run 或看清参数。 |
| `coding_orchestration/report_contract.py`、`report_admission.py` | 结构化 report 质量门，改动会影响任务是否可进入下一阶段。 |
| `coding_orchestration/state_machine.py`、`status_policy.py`、`models.py` | 公共状态 contract，改动需同步测试和用户可见文案。 |

## 不确定项

- 仓库没有 `pyproject.toml`、`requirements.txt` 或 lockfile；测试当前从文档和现有测试推断为标准库 `unittest`。
- Hermes CLI、Codex CLI、`lark-cli`、飞书权限和 Gateway 状态依赖本机环境，不属于本仓库可静态验证事实。
- `docs/` 下的流程图、部署指南、前置准备和交付流文档作为项目沉淀文档保留；superpowers 生成的执行计划统一放入可提交的 `docs/plans/`。
- 宣讲/demo 材料放入忽略的 `docs/local/presentations/`。
